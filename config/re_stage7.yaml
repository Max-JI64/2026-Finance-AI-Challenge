project:
  stage: RE Stage 7
  contract_version: re7-contract-v1
  status: full_contract_approved_implementation_not_started
  approved_at: "2026-08-16"

inputs:
  cashflow_engine: re3-v1
  policy_event_engine: re4-v1
  market_scenario_model: re5-lightgbm-quantile-v1
  eligibility_engine: re6-v1
  direct_shock_fallback: true

candidate_routing:
  actionable:
    eligibility_status: 입력 기준 적격 후보
    availability_status: 기준일상 접수 가능
    user_label: 지금 비교 가능
    default_ranking_allowed: true
    fast_execution_ranking_allowed: true
  conditional:
    eligibility_statuses: [입력 기준 적격 후보, 추가 확인 필요]
    availability_statuses: [기준일상 접수 가능, 접수 가능 여부 확인 필요]
    user_label: 확인 후 비교
    explicit_user_assumption_required: true
    default_ranking_allowed: false
    fast_execution_ranking_allowed: false
  excluded:
    eligibility_status: 부적격
    availability_status: 접수기간 종료
    user_label: 제외
    simulation_allowed: false
    ranking_allowed: false

user_boundary:
  visible_groups: [지금 비교 가능, 확인 후 비교, 제외]
  hidden_internal_fields: [rule_id, chunk_id, bm25_score, search_rank]
  conditional_action_label: 조건을 충족했다고 가정하고 비교

combination_compatibility:
  statuses: [조합 가능, 확인 필요, 조합 불가]
  default_combination_status: 조합 가능
  unknown_or_unverified_status: 확인 필요
  same_expense_double_funding_status: 조합 불가
  conditional_user_assumption_required: true
  conditional_default_ranking_allowed: false
  conditional_fast_execution_ranking_allowed: false

market_scenario_application:
  thirteen_week_target: target_a_next_quarter_yoy
  six_month_target: target_b_next_two_quarter_yoy
  default_application_ratio: 1.0
  default_result_label: 상권환경 변화율을 동일 비율로 반영한 참고 스트레스 계산
  user_adjustable_ratio: true
  direct_shock_override: true
  record_selected_ratio_and_assumptions: true
  personal_sales_prediction_claim_allowed: false

safe_cash:
  suggestion_horizon_days: 28
  suggestion_basis: 앞으로 4주 동안 반드시 지급해야 하는 비용
  detailed_input_basis: 향후 28일의 필수 지출 일정 합계
  simple_input_components: [월 고정비, 월 대출상환액, 필수 변동비 추정액]
  user_editable: true
  insufficient_input_action: 사용자 직접 입력 요청
  arbitrary_fixed_amount_allowed: false

implementation_authorized: false
