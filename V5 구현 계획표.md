# V5 구현 계획표

> 상태: V5 코드 Gate와 비시각 검증 완료, V5 Final 종료 경로 승인, 최종 화면·모바일 체감·발표 리허설과 제출 정리 대기
>
> 기준 버전: 현재 `v4/` 전체 서비스
>
> 구현 원칙: V4 실행 소스와 정적 자산을 독립 `v5/`로 복사하고 복사 직후 SHA-256 일치를 확인한 뒤, V5 복사본만 수정한다.
>
> 문서 역할: 새 세션에서 V5 구현을 시작할 때 사용하는 권위 계획서다. V2, V3, V4 문서는 각 버전의 이력과 복구 근거로 보존한다.

---

## 1. V5의 결론

V5는 V4에 AI 기능을 여러 개 덧붙이는 버전이 아니다. 사용자가 첫 화면에서 고른 해결 목적이 정책 검토 순서, 지표 강조, 공고 확인 순서까지 이어지도록 제품의 연결 구조를 다시 만드는 버전이다.

V4에는 이미 다음 기능이 있다.

- LightGBM 기반 상권 매출 하방, 기준, 회복 시나리오
- 공식 Rule 기반 자격 확인
- Event 기반 정책 금융효과 변환
- 13주 현금과 6개월 부채 계산
- 조건부 정책 효과와 확정 순위 분리
- Luna 기반 저장 공고 전체 구조화
- Chunk ID, 인용문, 숫자 근거 검증
- 외부 AI 실패 시 필드별 직접 확인과 로컬 경로

V4의 약점은 AI 기술의 수가 아니다. 첫 화면의 선택이 이후 결과에 거의 드러나지 않는다는 점이다. 현금 부족과 대출 상환 부담을 선택해도 안내 문구만 일부 달라지고, 비교 화면의 기본 정책과 지표 순서는 그 목적을 따르지 않는다.

V5의 핵심 흐름은 다음과 같다.

> 해결 목적 선택 -> 재무 진단 -> 정책 후보 선택 -> 정책 효과 비교 -> 선택 정책의 조건 확인 -> 지금 확인할 공고 항목 1개

사용자는 복잡한 에이전트 구조를 보지 않는다. 화면에는 하나의 `버팀AI`만 보인다. 내부에서는 제한된 도구와 결정론적 검증기가 각 역할을 분담한다.

---

## 2. V4를 기준 버전으로 선택한 이유

V5는 V3가 아니라 V4에서 시작한다.

V4에는 V3 이후 추가된 다음 자산이 이미 있다.

- 독립 5단계 서비스 셸
- 걱정 선택과 입력 원장
- 선택 정책 최대 3개의 통합 그래프
- 정책별 무대응 대비 차이
- 정책 탭과 한 정책 집중 보기
- 바우처, 대환대출, 재도전특별자금의 조건부 그래프
- 대환 실행시점 잔액 검증
- 조건과 답변 수정 경로
- 선택 정책 최대 3개의 전체 공고 Luna 구조화
- 필드별 근거 검증과 Fail-close
- 공고 원문 해시 기반 영구 캐시
- V4 전용 테스트와 검증 기록

V3에서 다시 시작하면 위 기능을 재이식해야 하고 CSS와 상태 흐름도 다시 맞춰야 한다. V4를 복사하면 기존 화면의 시각적 연속성과 검증된 정책 비교 흐름을 그대로 이어갈 수 있다.

V4는 V5의 백업이자 비교 기준으로 남는다. V5 구현을 위해 `v4/`를 직접 수정하지 않는다.

---

## 3. 현재 V4 기준선

V5 구현 전 다음 V4 상태를 현재 기준선으로 재확인한다.

| 항목 | V4 기준선 |
| --- | --- |
| 앱 엔트리 | `v4.main:app` |
| API 버전 | `v4-api-v1.0` |
| 기본 포트 | `8002` |
| 정적 자산 | `v4/static/` |
| 브라우저 저장 | `sessionStorage` |
| 공고 원본 | `rag/index/policy_re8.sqlite3` |
| 공고 추출 캐시 | `v4/runtime/notice_extraction_cache.sqlite` |
| 문서 업로드·OCR | 미지원 |
| 사용자 재무 장기 저장 | 미지원 |
| 자동 신청·기관 전송 | 미지원 |
| 최신 V4 회귀 | 16/16 통과 기록 |
| 관련 공유/V4 회귀 | 71/71 통과 기록 |
| JavaScript 문법 | 2개 파일 통과 기록 |

이 표는 과거 기록을 그대로 믿기 위한 것이 아니다. 새 구현 세션에서 파일, 테스트, `/health` 응답으로 다시 확인한다. 현재 상태가 달라졌다면 V5 복사 전에 차이를 기록한다.

---

## 4. V5 제품 정의

### 4.1. 사용자에게 보이는 정의

> 내 가게의 현금과 대출 상태를 진단하고, 해결하고 싶은 문제에 맞춰 정책과 재무 숫자의 검토 순서를 정한 뒤, 선택한 정책의 자격조건과 13주 현금·6개월 부채 변화, 지금 확인할 공고 항목을 보여주는 서비스

### 4.2. 심사위원에게 설명할 정의

> 버팀AI V5는 사용자의 해결 목적과 재무 상태를 검토 렌즈로 바꾸고 정책과 지표의 확인 순서를 조정한다. 사용자가 정책을 고르면 그 정책의 검수 조건을 준비 화면에서 직접 선택한다. LightGBM은 상권 단위 매출 시나리오를 만들고, 공식 Rule·Event·현금흐름 엔진은 자격·금액·효과·순위를 계산한다. Luna는 저장된 공개 공고 전체에서 신청 핵심정보를 구조화하며, 서버는 Chunk·인용·숫자 근거를 검증한다. AI는 검토 순서와 근거 정리를 담당하고 금융 판단값은 결정론적 엔진에 고정한다.

### 4.3. 대회 발표용 한 문장

> 사용자가 고른 문제를 AI 검토 계획으로 바꾸고, 선택한 정책의 조건을 직접 확인하면서 현금·부채 효과와 다음 공고 확인을 근거와 함께 제시합니다.

### 4.4. 경쟁서비스와 구분되는 지점

V5의 차별점은 정책 목록이나 범용 챗봇이 아니다.

- 정책이 내 가게의 현금과 부채를 어떻게 바꾸는지 계산한다.
- 선택한 정책의 자격조건을 한 화면에서 직접 확인하고 바로 다시 계산한다.
- 사용자의 해결 목적에 따라 검토 순서와 지표를 바꾼다.
- 순위와 검토 순서를 분리해 사용자가 오해하지 않게 한다.
- 공고에서 찾은 값은 원문 근거 검증을 통과해야 표시한다.
- 외부 AI가 실패해도 진단과 비교를 끝낼 수 있다.

---

## 5. V5에서 지키는 금융 권한 경계

### 5.1. 결정론적 엔진이 결정하는 값

다음 값은 AI가 생성하거나 수정하지 않는다.

- 정책 후보의 공식 자격 Rule 결과
- 신청 가능 여부와 현재 접수 여부의 구분
- 정책 금액과 사용자가 입력한 시나리오 금액
- Event 종류와 적용일
- 13주 현금 잔액
- 6개월 부채 잔액
- 월 상환액과 신규 부채
- 정책 효과의 계산 가능 여부
- 조건부 정책의 `ranking_eligible=False`
- 기존 결정 엔진의 순위와 Pareto 결과

### 5.2. AI와 제한형 오케스트레이터가 담당하는 값

- 사용자가 선택한 해결 목적을 검토 렌즈로 변환
- 재무 결과에서 검토 신호 구성
- 선택 정책에 연결된 검수 조건 구성
- 정책 검토 순서와 기본 초점 결정
- 같은 계산값의 설명 순서와 지표 강조
- 저장 공고 전체에서 신청 핵심정보 구조화
- 확정된 사실만 사용하는 설명문 작성
- 외부 AI 실패 시 로컬 설명과 직접 확인 경로 선택

### 5.3. 검토 순서와 추천 순위의 분리

V5는 다음 두 순서를 별도 필드로 관리한다.

| 필드 | 의미 | 변경 권한 |
| --- | --- | --- |
| `rank_position` | 기존 결정 엔진이 계산한 정책 순위 | 기존 결정론 엔진만 변경 |
| `review_position` | 사용자의 해결 목적에 따라 먼저 살펴볼 순서 | V5 검토 렌즈가 변경 가능 |

화면은 `검토 1/3`처럼 표시한다. `추천 1위` 또는 `가장 좋은 정책`이라는 표현을 사용하지 않는다.

---

## 6. V5 화면 구조

내부 계산과 상태는 V4의 5단계를 이어받는다. 상단 진행 표시는 3개 사용자 단계로 단순화한다.

| 사용자 단계 | 내부 화면 | 사용자의 질문 |
| --- | --- | --- |
| 진단 | 기존 1·2·3단계 | 내 가게는 어떤 상태이고 무엇부터 확인해야 하는가 |
| 비교 | 기존 4단계 | 어떤 정책이 현금과 부채를 어떻게 바꾸는가 |
| 준비 | 기존 5단계 | 선택한 정책에서 지금 무엇을 확인해야 하는가 |

상단에는 `진단 -> 비교 -> 준비`만 표시한다. 현재 내부 위치는 제목과 보조 문구로 알려준다.

이 구조는 기존 5단계 상태와 API 연결을 보존하면서 사용자가 단계 수를 부담스럽게 느끼지 않도록 한다.

---

## 7. 첫 화면과 해결 목적

### 7.1. Hero 축소

V4의 큰 Hero는 첫 화면의 실제 행동을 아래로 밀어낸다. V5에서는 브랜드와 서비스 설명을 한 줄짜리 안내 영역으로 줄인다.

권장 문구:

> 13주 현금과 6개월 부채를 비교해 지금 먼저 볼 정책을 찾습니다.

첫 화면의 첫 Viewport 안에 다음 요소가 보여야 한다.

- 상권·업종 선택 시작점
- 주된 해결 목적 선택
- 다음 단계 버튼

### 7.2. 사업장과 업종

다음 V4 자산을 그대로 유지한다.

- 자치구, 행정동, 상권 원형 지도
- 목록과 지도 선택 동기화
- 공식 상권 코드와 업종 코드
- 키보드와 스크린리더 상태
- 선택 초기화와 상위 단계 복귀

### 7.3. 주된 해결 목적 한 개

기존 `걱정 최대 두 가지`를 다음 단일 선택으로 교체한다.

| 값 | 화면 문구 | 검토 렌즈 |
| --- | --- | --- |
| `cash_runway` | 이번 달 현금이 버틸지 확인 | 현금 생존 |
| `debt_relief` | 대출 상환 부담 줄이기 | 부채 부담 |
| `fixed_cost` | 고정비를 줄일 방법 찾기 | 비용 절감 |
| `policy_choice` | 지원금과 융자 중 무엇을 볼지 비교 | 정책 유형 비교 |
| `unsure` | 무엇부터 확인할지 모르겠음 | 진단 후 제안 |

화면 제목:

> 오늘 가장 먼저 해결하고 싶은 것은 무엇인가요?

안내 문구:

> 이 선택은 자격이나 지원금액을 바꾸지 않습니다. 어떤 정책과 재무 숫자를 먼저 보여줄지 정합니다.

선택은 한 개만 허용한다. 다른 항목을 누르면 기존 선택을 교체한다. 선택하지 않은 상태에서는 다음 단계로 진행하지 않고, 무엇을 고를지 모르는 사용자는 `무엇부터 확인할지 모르겠음`을 선택할 수 있다.

### 7.4. `매출 감소` 처리

`매출 감소`는 해결 목적에서 제거한다. 최근 월매출 입력으로 확인할 수 있는 재무 신호이기 때문이다.

2단계 계산 후 다음처럼 표시한다.

- 최근 매출 감소 감지
- 최근 매출 유지
- 최근 매출 회복
- 비교 기간 부족으로 방향 미확인

이 신호는 사용자의 목적을 자동으로 덮어쓰지 않는다. 검토 계획에 보조 근거로만 사용한다.

### 7.5. 발표 프리셋 분리

일반 경로에서는 발표 프리셋을 숨긴다. `/?demo=1`로 진입한 경우에만 `발표용 예시 불러오기` 영역을 표시한다.

프리셋은 입력만 채운다. 다음 동작을 자동 수행하지 않는다.

- 해결 목적 자동 선택
- 계산 실행
- 정책 자동 선택
- 다음 단계 이동
- 외부 AI 호출

발표자는 예시를 불러온 뒤 해결 목적을 직접 선택하고 같은 정상 흐름을 거친다.

### 7.6. 운영 기간 삭제

V4의 `운영 기간` 입력은 현재 계산과 정책 Context에 연결되지 않는다. V5에서는 DOM, 상태, 검증, Snapshot에서 제거한다.

향후 공식 Rule이나 계산에 실제로 필요해지면 `개업일` 또는 검증된 다른 필드로 다시 설계한다. 받기만 하는 입력은 만들지 않는다.

---

## 8. 재무 입력과 자동 감지 신호

### 8.1. 유지하는 입력 카드

- 최근 월매출 6개월, 최대 12개월
- 현재 현금
- 임대료
- 인건비
- 매입비
- 기타 고정비
- 대출 잔액
- 금리
- 남은 기간
- 대출 없음
- 기존 거래내역 CSV
- 선택형 대출 CSV

금액 단위, CSV 인코딩, 입력 원장, 오류와 `확인 권장` 기준은 V4를 유지한다.

### 8.2. 진단 전 입력 원장

계산 전에 다음을 한 화면에서 확인한다.

- 상권과 업종
- 주된 해결 목적
- 월별 매출과 날짜
- 현재 현금과 비용
- 대출 상태
- 적용할 시기 가정
- 차단 오류
- 비차단 확인 권장

사용자가 `이 값으로 진단`을 눌러야 계산을 시작한다.

### 8.3. 자동 감지 신호

V5는 기존 계산값으로 검토 신호를 만든다. 별도 예측값을 만들지 않는다.

| 신호 | 근거 |
| --- | --- |
| `cash_gap_28d` | 현재 현금과 앞으로 28일 필요현금 차이 |
| `negative_cash_within_13w` | 13주 기준선에서 첫 음수 주차 존재 여부 |
| `sales_direction` | 기존 최근 매출 방향 계산 |
| `high_interest_debt_present` | 입력 대출 금리와 대환 검토 계약의 조건 |
| `fixed_cost_pressure` | 임대료·인건비·매입비·기타 고정비와 매출 관계 |
| `new_debt_sensitivity` | 선택 목적과 신규 부채 변화 지표의 관련성 |
| `policy_route_uncertainty` | 정책 후보에 남은 고영향 미확인 Rule 수 |

새로운 임계값이 필요한 경우 구현 세션은 임의 숫자를 정하지 않는다. 기존 계약으로 계산할 수 없는 신호는 `미확인`으로 두고 사용자에게 선택지를 제시한 뒤 승인받는다.

### 8.4. `unsure` 처리

사용자가 `무엇부터 확인할지 모르겠음`을 선택한 경우 V5가 검토 렌즈 후보를 하나 제안한다.

우선순위:

1. 28일 필요현금 부족 또는 13주 내 현금 음수
2. 대환 검토가 가능한 고금리 대출
3. 고정비 부담 신호
4. 정책 유형 비교

제안은 자동 확정하지 않는다. 화면에 `이 기준으로 검토`와 `다른 기준 선택`을 제공하고 사용자가 확인한 뒤 진행한다.

---

## 9. AI 검토 계획

### 9.1. 표시 시점

입력 원장 확인과 첫 진단 계산이 끝난 뒤, 정책 후보와 함께 표시한다.

### 9.2. 카드 내용

AI 검토 계획에는 다음 세 항목이 있어야 한다.

1. 사용자가 선택한 해결 목적
2. 재무 입력에서 확인한 신호
3. 먼저 비교할 정책 효과와 지표

대출 상환 부담 예시:

> 대출 부담을 먼저 보기로 했습니다. 현재 자료에서는 28일 현금 부족과 대환 검토 대상 대출이 함께 보입니다. 대환 정책을 첫 비교로 배치하고 신규 부채가 생기는 정책과 함께 보겠습니다.

### 9.3. 수정 권한

사용자는 `검토 기준 바꾸기`로 첫 화면의 다섯 렌즈 중 하나를 다시 선택할 수 있다.

렌즈를 바꾸면 다음 값만 다시 계산한다.

- 정책 검토 순서
- 기본 초점 정책
- 지표 표시 순서
- 공고 필드 확인 순서
- 설명 문구

다음 값은 다시 계산하거나 변경하지 않는다.

- 사용자 재무 입력
- 공식 자격 결과
- 정책 금액
- 정책 효과
- 기존 순위 점수와 순위

### 9.4. 문장 생성 방식

P0에서는 검토 계획을 검증된 로컬 템플릿으로 만든다. 사용자 재무정보를 외부 AI로 보내지 않는다.

Luna를 사용한 문장 다듬기는 P1 후보다. 도입하려면 전송할 비식별 필드, 동의 화면, 보유 여부와 Fallback을 별도 승인받는다.

---

## 10. 정책별 자격조건 선택

### 10.1. 목표

진단과 비교 사이에는 별도 질문 단계를 두지 않는다. 사용자는 정책 후보를 먼저 확인하고 최대 3개를 선택한다. 준비 화면에서는 현재 선택한 정책에 필요한 자격조건을 한 번에 보고 직접 선택한다.

### 10.2. 선택 항목 출처

선택 항목을 자유 생성하지 않는다. 다음 검수된 출처에서만 만든다.

- 기존 정책별 질문 카탈로그
- 공식 Rule의 미확인 입력 필드
- Event 계산에 필요한 검수된 금액과 날짜 필드
- 정책 세부 유형처럼 계산 경로를 여는 검수된 필드

### 10.3. 선택 즉시 재계산

사용자가 `예`, `아니오`, `모름` 또는 검수된 값을 고르면 같은 준비 화면을 유지한 채 다음 항목을 다시 계산한다.

- 자격 상태 변화
- 계산 가능 여부 변화
- 조건부 그래프 생성 여부 변화
- Event 금액·날짜 변화 여부

확률은 만들지 않는다. `예일 가능성`, `승인 가능성`, `정책 성공 확률`을 추정하지 않는다.

### 10.4. 사용자 선택과 기관 확인 분리

사용자가 자신의 상황으로 답할 수 있는 조건만 선택지로 둔다. 현재 접수 여부, 잔여 예산, 기관 심사처럼 사용자가 확정할 수 없는 내용은 `기관에서 확인할 항목`으로 분리한다. `모름`은 추정하지 않으며 해당 정책은 추가 확인 상태로 유지한다.

---

## 11. 정책 후보와 비교 화면

### 11.1. 후보 정책

다음 V4 계약을 유지한다.

- 최대 3개 정책 선택
- 공식 자격과 신청 가능 여부 분리
- 구조적 제외와 보완 가능 조건 분리
- 계산 가능한 조건부 정책만 점선 그래프 제공
- 조건부 정책은 순위 제외
- 정책 금액이나 날짜가 없으면 임의 기본값 생성 금지
- 선택하지 않은 정책을 자동 신청 대상으로 처리하지 않음

### 11.2. 검토 순서 계산

사용자가 선택한 최대 3개 정책 안에서만 `review_position`을 계산한다.

| 검토 렌즈 | 먼저 보는 금융 메커니즘 |
| --- | --- |
| 현금 생존 | 보조금·바우처·정책융자 중 13주 현금 개선이 확인되는 정책 |
| 부채 부담 | 대환·이자지원·신규 부채가 적은 정책 |
| 비용 절감 | 바우처·비용 절감·비용 상쇄 정책 |
| 정책 유형 비교 | 서로 다른 금융 메커니즘을 비교할 수 있는 순서 |
| 진단 후 제안 | 사용자가 확인한 최종 검토 렌즈와 같은 규칙 |

같은 메커니즘 안에서는 기존 후보 순서를 유지한다. 새 점수를 만들지 않는다.

`review_position`은 선택 정책을 보는 순서만 바꾼다. 후보 검색 결과, 공식 자격, 정책 금액, 계산 결과와 `rank_position`은 그대로다.

### 11.3. 기본 초점

4단계 진입 시 `review_position=1` 정책을 기본으로 연다.

상단에 이유를 표시한다.

> 대출 부담을 먼저 보기로 했기 때문에 대환대출을 첫 비교로 배치했습니다. 전체 후보와 계산값은 바뀌지 않았습니다.

사용자는 정책 탭과 좌우 버튼으로 다른 정책을 바로 볼 수 있다.

### 11.4. 렌즈별 지표 순서

| 검토 렌즈 | 첫 번째 | 두 번째 | 세 번째 |
| --- | --- | --- | --- |
| 현금 생존 | 13주 뒤 현금 차이 | 13주 최저 현금 개선 | 28일 필요현금 부족 변화 |
| 부채 부담 | 월 최대상환 변화 | 6개월 부채 변화 | 신규 부채 변화 |
| 비용 절감 | 월 고정비 변화 | 13주 뒤 현금 차이 | 신규 부채 변화 |
| 정책 유형 비교 | 13주 뒤 현금 차이 | 신규 부채 변화 | 월 최대상환 변화 |

기존 계산 응답에 값이 없으면 새 값을 추정하지 않는다. 지원되지 않는 지표는 `계산 근거 없음`으로 닫고 V4의 기존 세 지표를 사용한다.

### 11.5. 그래프

V4 그래프를 유지한다.

- 무대응 실선
- 선택 정책 최대 3개
- 조건부 정책 점선
- 28일 필요현금 미달 구간
- 현금 적자 구간
- 범례와 선택 정책 강조
- 그래프가 승인 가능성을 뜻하지 않는다는 안내

그래프의 선 순서와 색을 `review_position`에 맞출 수 있지만 계산 데이터 순서는 바꾸지 않는다.

### 11.6. 정책 카드 문구

각 정책은 다음 정보만 우선 표시한다.

- 정책명
- 현재 상태 하나
- 검토 순서와 이유
- 렌즈에 맞춘 지표 3개
- 무대응 대비 차이
- `이 정책 확인 준비로 이동`

정책마다 같은 자격 고지와 일반 경고를 반복하지 않는다. 공통 경고는 비교 화면 상단에 한 번 표시한다.

### 11.7. What-if

What-if는 비교 화면 아래의 접힌 보조 기능으로 유지한다.

지원 범위:

- 매출 변화
- 비용 감소
- 상권 시나리오 변경
- 기존 검증된 목표 변경

금지 범위:

- 비용 증가를 AI가 임의 해석
- 정책 자격 변경
- 지원금액 추정
- 승인 가능성 추정
- 지원하지 않는 정책 효과 생성

기존 한 번의 명확화 제한과 로컬 검증을 유지한다.

---

## 12. 신청 준비와 다음 확인

### 12.1. 5단계의 역할

5단계는 신청 절차 전체를 대신하지 않는다. 다음 세 가지를 한 화면에서 연결한다.

- 현재 답변 기준의 정책 조건
- Luna가 저장 공고에서 구조화한 신청 핵심정보
- 지금 먼저 확인할 공고 항목 하나

### 12.2. 조건 영역

V4의 조건 영역을 유지한다.

- 구조적 제외조건
- 보완 가능한 조건
- 앞에서 입력한 답변
- 답변 수정
- 공식 확인 필요 상태
- 입력한 답변 기준 제외조건이 없다는 제한적 문구

구조적 제외 정책에는 임의 효과나 신청 준비 완료 상태를 만들지 않는다.

### 12.3. 공고 구조화 필드

V4의 여섯 필드를 유지한다.

| 키 | 화면 문구 |
| --- | --- |
| `publication_date` | 게시일 |
| `application_period` | 신청 기간 |
| `application_path` | 신청 경로 |
| `financing_terms` | 융자·지원 조건 |
| `required_documents` | 필요 서류 |
| `contact` | 문의처 |

필드 수와 Schema를 바꾸려면 공고 추출 평가, 캐시 스키마와 기존 결과 무효화 영향을 먼저 제시하고 승인받는다.

### 12.4. 다음 확인 한 개

필드 전체를 처음부터 펼치지 않는다. 다음 규칙으로 한 항목만 `다음 확인` 카드에 표시한다.

공통 우선순위:

1. 신청 기간과 현재성
2. 사용자의 검토 렌즈와 직접 관련된 금융조건
3. 신청 경로
4. 필요 서류
5. 문의처
6. 게시일

렌즈별 두 번째 항목:

| 검토 렌즈 | 우선 금융조건 |
| --- | --- |
| 현금 생존 | 지원 시점·지원액·융자 실행 조건 |
| 부채 부담 | 대환 한도·금리·기간·기존 대출 조건 |
| 비용 절감 | 사용 가능 비용·지원 한도·정산 조건 |
| 정책 유형 비교 | 지원 방식·상환 의무·신규 부채 여부 |

필드를 확인하면 다음 미확인 항목이 `다음 확인`으로 올라온다. 나머지는 `남은 확인 n개` 아래에 접어서 보여준다.

### 12.5. 공고에서 찾지 못한 값

다음 상태를 구분한다.

- 공고에 값이 없음
- Luna가 값을 찾지 못함
- 인용 근거 검증 실패
- 숫자 근거 검증 실패
- 외부 AI 호출 실패
- 저장 공고의 최신성 확인 필요

찾지 못한 값을 빈칸으로 채우지 않는다. `공고에서 확인하지 못한 항목 n개`로 묶고 공식 공고 링크를 제공한다.

### 12.6. 출처와 최신성

사용자에게 다음 정보만 간결하게 보여준다.

- 저장 공고 기준일
- 공고 버전 또는 원문 해시 식별 상태
- 캐시 재사용 또는 새 분석 여부
- 공식 공고 열기
- 현재 접수 여부와 잔여 예산은 별도 확인이 필요하다는 안내

원문 Chunk 전체와 긴 인용문은 화면에 반복하지 않는다. 서버 내부 검증에는 계속 사용한다.

### 12.7. 확인 상태

사용자가 직접 누른 필드 확인 상태만 브라우저 `sessionStorage`에 저장한다.

확인 키:

- 정책 ID
- 정책 버전
- 공고 원문 해시
- 필드 키

공고 원문 해시가 바뀌면 과거 확인 상태를 새 공고에 자동 적용하지 않는다.

### 12.8. 정책 상담

정책 상담은 접힌 보조 기능으로 남긴다.

- 현재 선택 정책 범위만 검색
- 공개 공고 근거만 사용
- 사용자 재무정보 외부 전송 금지
- 자격·금액·효과·순위 변경 금지
- 평문 답변
- 외부 AI 실패 시 로컬 근거와 공식 링크 제공

정책 상담을 V5의 첫 AI 기능처럼 강조하지 않는다.

---

## 13. V5 내부 AI 구조

### 13.1. 하나의 코디네이터

사용자 세션 상태는 하나의 V5 코디네이터가 관리한다. 여러 에이전트가 같은 금융 상태를 각각 수정하게 하지 않는다.

코디네이터의 허용 작업:

1. 검토 렌즈 확인
2. 기존 재무 계산 호출
3. 감지 신호 구성
4. 기존 정책 검색과 Rule 평가 호출
5. 정책별 준비 조건 구성
6. 기존 시뮬레이터 호출
7. 검토 순서 구성
8. 공고 추출 호출
9. 검증된 사실 설명

### 13.2. 도구 계약

| 도구 | 입력 | 출력 | 변경 금지 |
| --- | --- | --- | --- |
| `finance_diagnosis` | 사용자 입력과 상권 시나리오 | 13주·6개월 기준선 | 입력값·계산식 임의 수정 |
| `review_lens` | 사용자 목적과 감지 신호 | 검토 계획 | 자격·금액·순위 수정 |
| `policy_preparation` | 선택 정책과 검수된 조건 카탈로그 | 정책별 준비 선택지 | 자유 조건 생성 |
| `policy_discovery` | 기존 상황 Context | 후보와 공식 상태 | AI 임의 후보 삽입 |
| `policy_simulator` | 검증된 Event 입력 | 현금·부채 대안 | 지원금·금리 추정 |
| `notice_extractor` | 공개 저장 공고 전체 | 여섯 구조화 필드 | 사용자 데이터 전송 |
| `fact_locked_explainer` | 확정된 수치와 근거 | 사용자 설명 | 수치·상태·순위 변경 |

### 13.3. 멀티에이전트 도입 기준

P0에서는 멀티에이전트를 도입하지 않는다.

다음 조건이 생기고 단일 코디네이터보다 품질 개선이 평가로 확인될 때만 검토한다.

- 독립된 공고 여러 개를 장시간 병렬 검토
- 각 역할의 결과를 별도 Oracle로 평가 가능
- 한 역할의 실패가 다른 역할의 금융 상태를 오염시키지 않음
- 역할 분리로 정확도나 지연시간이 실제 개선됨
- 사용자 화면에는 여전히 하나의 버팀AI만 보임

### 13.4. LangGraph 도입 기준

P0에서는 LangGraph로 이전하지 않는다.

다음 요구가 실제로 발생할 때 검토한다.

- 서버 재시작 뒤 장기 작업 재개
- 비동기 공고 변경 확인
- 사람 승인 대기 상태의 영속화
- 복잡한 재시도와 보상 동작
- 현재 상태 머신으로 관리하기 어려운 분기

프레임워크 이름은 사용자 화면과 기능 설명의 중심에 두지 않는다.

---

## 14. 하네스 엔지니어링 계약

V5의 AI 경쟁력은 도구 수보다 통제와 평가에서 보여준다.

### 14.1. 상태 Schema

다음 상태는 엄격한 Pydantic 또는 동등한 Schema로 관리한다.

- `ReviewLens`
- `DetectedSignal`
- `ReviewPlan`
- `PolicyReviewItem`
- `NoticeFieldPriority`
- `ToolExecutionTrace`

모든 Schema는 추가 필드를 금지하고 문자열·목록 길이, 금액 범위, 날짜 형식을 제한한다.

### 14.2. 도구 권한

- 정책 준비 도구는 공용 정책 계약에 연결된 검수 조건만 반환한다.
- 검토 렌즈는 표시 순서와 설명 순서만 바꾼다.
- 공고 추출기는 공개 공고 필드만 반환한다.
- 설명기는 전달받은 수치와 상태를 바꾸지 않는다.
- 어떤 AI 도구도 공용 금융 엔진 파일을 쓰지 않는다.
- 외부 AI 도구는 사용자 재무와 답변을 받지 않는다.

### 14.3. 실행 Trace

도구 실행마다 다음 메타정보를 남긴다.

- 세션 내 Trace ID
- 도구 이름과 버전
- 입력 Schema 버전
- 출력 Schema 버전
- 실행 시작과 종료 시각
- 성공, Fallback, Fail-close 상태
- 사용한 정책 ID와 공고 원문 해시
- 사용자에게 표시한 결정 이유

Trace에는 원문 사용자 재무표, 자유 메모, 인증정보를 저장하지 않는다.

### 14.4. 실패 처리

| 실패 | 처리 |
| --- | --- |
| 정책별 조건 구성 오류 | 값을 추정하지 않고 기관 확인 상태로 유지 |
| 화면에서 선택할 조건 없음 | 기관 확인 항목과 공고 확인으로 진행 |
| Luna 시간초과 | 해당 공고 직접 확인 |
| Luna Schema 오류 | 전체가 아니라 해당 응답 Fail-close |
| 인용 검증 실패 | 해당 필드만 직접 확인 |
| 숫자 검증 실패 | 해당 필드만 직접 확인 |
| 캐시 손상 | 캐시 무시 후 새 분석 또는 직접 확인 |
| LightGBM 불가 | 기존 사용자 직접 시나리오 경로 |

### 14.5. 재시도

- 공고 추출 자동 재시도는 기존 제한을 유지한다.
- 실패 결과를 영구 성공 캐시로 저장하지 않는다.
- 사용자가 `공고 다시 분석`을 누른 경우에만 캐시를 우회한다.
- 중복 클릭은 같은 정책·버전·원문 해시 요청 하나로 합친다.
- 금융 계산은 외부 AI 재시도와 독립적으로 끝나야 한다.

---

## 15. API 계약

### 15.1. V5 기본 경로

V4 API를 복사한 뒤 접두사를 `/api/v5`로 바꾼다.

- `GET /api/v5/catalog/areas`
- `GET /api/v5/catalog/industries`
- `GET /api/v5/catalog/policies`
- `GET /api/v5/market-scenarios/{area_code}/{industry_code}`
- `POST /api/v5/orchestrate`
- `POST /api/v5/what-if`
- `POST /api/v5/ai/ask`
- `POST /api/v5/application/notice-extract`
- `POST /api/v5/change/reconcile`

### 15.2. 오케스트레이션 요청 확장

`POST /api/v5/orchestrate` 요청에 다음 값을 추가한다.

- `review_lens`
- `review_lens_source`
- `confirmed_review_lens`
- `answered_fields`
- `asked_fields`
- `question_round`

마지막 세 필드는 이전 브라우저 세션과의 요청 호환을 위해 받지만 앞단 질문 게이트를 열지 않는다. 사용자 재무 입력과 기존 비교 요청 구조는 V4 계약을 유지한다.

### 15.3. 오케스트레이션 응답 확장

응답에 다음 값을 추가한다.

- `detected_signals`
- `review_plan`
- `review_order`
- 후보별 `preparation_questions`

`next_question`과 `question_trace`는 이전 응답 계약과의 호환을 위해 `null`로 반환한다.
- `focused_policy_id`
- `metric_order`
- `authority_invariants`

`authority_invariants`는 같은 요청에서 렌즈 변경 전후의 다음 값이 같음을 검증하기 위한 해시 또는 비교 가능한 요약이다.

- 후보 정책 ID 집합
- 자격 결과
- 정책 시나리오 입력
- 대안 계산 결과
- 순위 결과

### 15.4. 제거 후보 API

V4의 다음 API는 V5 UI의 실제 호출 여부를 먼저 검색한다.

- `/api/v4/situation/interpret`
- `/api/v4/ai/action-brief`

V5 DOM과 JavaScript에서 호출하지 않고 테스트 의존성도 없다면 V5 복사본에서 제거한다. V4 API는 그대로 둔다.

### 15.5. Health와 Scope

`/health`와 `/scope`는 다음 상태를 명시한다.

- `api_version: v5-api-v1.0`
- `review_lens: enabled`
- `decision_impact_questions: enabled`
- `policy_review_order: lens_only`
- `deterministic_financial_authority: true`
- `application_document_upload: not_supported`
- `personal_data_persistence: session_only`
- `notice_extraction_cache: local_persistent_public_notice_only`
- `multi_agent_runtime: not_used`
- `langgraph_runtime: not_used`

---

## 16. 브라우저 상태와 저장

### 16.1. V5 전용 세션키

V5는 `buttimaiv5:session:v1`을 사용한다. V4의 `buttimaiv4:session:v3`를 읽거나 덮어쓰지 않는다.

### 16.2. Snapshot 필드

- 선택 상권과 업종
- 확인된 검토 렌즈
- 재무 입력
- 대출 없음 상태
- 정책별 조건 선택값
- 선택 정책
- 기본 초점 정책
- What-if 상태
- 공고 필드 확인 상태

### 16.3. 저장 금지

- 외부 AI 원문 요청 전체
- 자유 입력 메모
- API 키와 인증정보
- 사용자 재무정보의 서버 DB 저장
- 승인되지 않은 정책 변경값
- AI가 생성한 승인 가능성

### 16.4. 공고 추출 캐시

V5는 `v5/runtime/notice_extraction_cache.sqlite`를 사용한다. V4 캐시 DB를 복사하지 않는다.

캐시 키는 V4와 같이 다음을 포함한다.

- 정책 ID
- 정책 버전
- 공고 원문 해시
- 모델
- Schema 버전

공개 공고 원본 DB `rag/index/policy_re8.sqlite3`는 읽기 전용으로 공유한다.

---

## 17. 개인정보와 외부 AI

### 17.1. 서버 처리

사용자 재무 입력은 계산 요청 동안 서버 메모리에서 처리할 수 있지만 장기 저장하지 않는다.

### 17.2. Luna 공고 구조화

외부 AI에 보낼 수 있는 값:

- 선택 정책의 공개 저장 공고 전체
- 정책 ID와 정책명
- 정책 버전
- Chunk ID
- 공개 출처 URL

외부 AI에 보내지 않는 값:

- 사업장 위치 선택
- 업종
- 월매출
- 현재 현금
- 비용
- 대출
- 정책별 조건 선택값
- 현금·부채 계산값
- 확인 메모
- 필드 확인 상태

### 17.3. 외부 데이터 획득

V5 P0는 새 외부 데이터를 요구하지 않는다.

향후 새 공고나 데이터가 필요하면 Codex는 이유, 시점, 정확한 파일·기간·API 항목, 공식 링크, 권장 저장 경로를 먼저 알린다. 사용자가 직접 내려받아 프로젝트에 적재한 뒤에만 처리한다.

### 17.4. 계속 제외하는 기능

- 신청서류 업로드
- OCR
- 계좌·카드·POS 자동 연결
- 사용자 재무 장기 저장
- 실시간 공고 크롤링
- 자동 알림
- 기관 전송
- 자동 신청
- 승인 확률 예측
- 개인 폐업 확률 예측

---

## 18. CSS와 시각적 연속성

### 18.1. 복사할 CSS

- `v4/static/styles.css`
- `v4/static/v3-extension.css`
- `v4/static/v4-extension.css`

복사 직후 해시 일치를 기록한다.

### 18.2. V5 CSS 계층

복사 뒤 `v4-extension.css`를 `v5-extension.css`로 이름을 바꾸고 V5 수정은 이 파일에 우선 작성한다.

`styles.css`와 `v3-extension.css`는 다음 이유가 없으면 수정하지 않는다.

- 공통 토큰 오류
- 접근성 오류
- V5 전용 Override로 해결할 수 없는 구조 문제

기존 CSS를 일괄 교체하지 않는다. Tailwind, Bootstrap 또는 다른 디자인 프레임워크로 덮어쓰지 않는다.

### 18.3. 유지할 시각 요소

- 버팀AI 브랜드
- 어두운 신뢰 중심 색상 체계
- 카드, 배지, 버튼과 입력 필드의 기본 모양
- 원형 지도와 단계별 선택 상태
- 13주 그래프 색과 필요현금 구간
- 조건부 점선
- 오류, 확인 권장, 공식 확인 필요 상태 색
- 데스크톱과 모바일 반응형 기준

### 18.4. V5에서 바꿀 요소

- 큰 Hero를 얇은 안내 영역으로 축소
- 상단 5단계 표시를 `진단·비교·준비`로 단순화
- 걱정 다중 선택을 해결 목적 단일 선택으로 교체
- AI 검토 계획 카드 추가
- 정책 기본 초점의 이유 표시
- 5단계에서 다음 확인 한 개를 우선 노출
- 나머지 공고 필드는 접힌 목록으로 이동
- 중복 고지와 중복 설명 제거

### 18.5. 시각 승인 경계

코드는 다음을 검증할 수 있다.

- DOM 구조
- CSS 선택자 연결
- 숨김과 펼침 상태
- 키보드 포커스
- 접근성 속성
- 반응형 규칙
- JavaScript 상태

최종 화면의 미감, 발표 화면 밀도와 모바일 체감은 사용자가 직접 승인한다. 사용자 승인 없이 코드 검증을 시각 승인으로 보고하지 않는다.

---

## 19. 접근성과 오류 상태

### 19.1. 키보드

- 해결 목적 버튼은 화살표 또는 Tab으로 이동 가능
- 선택 상태는 `aria-pressed` 또는 Radio 의미로 노출
- 정책 조건 선택 뒤 같은 조건의 선택 상태로 포커스 복귀
- 검토 기준 변경 뒤 검토 계획 제목으로 포커스 이동
- 정책 탭은 `role=tablist`, `role=tab`, `aria-selected` 유지
- 다음 확인 완료 뒤 다음 카드 제목으로 포커스 이동
- Dialog는 닫은 뒤 원래 버튼으로 포커스 복귀

### 19.2. Screen reader

- 검토 순서 변경 이유를 한 번만 알림
- 계산 중 경과시간을 매초 반복 낭독하지 않음
- 그래프의 핵심 값을 텍스트로 함께 제공
- 조건부 그래프가 승인 가능성을 뜻하지 않음을 제공
- 공고 필드 성공, 누락, 검증 실패를 서로 다른 텍스트로 제공

### 19.3. 빈 상태

| 상태 | 사용자 안내 |
| --- | --- |
| 해결 목적 미선택 | 다섯 선택지 중 하나를 선택하도록 안내 |
| 정책 후보 없음 | 입력 확인과 공식 정책 검색 경로 제공 |
| 화면에서 선택할 정책 조건 없음 | 기관 확인 항목과 공고 확인으로 진행한다는 안내 |
| 선택 정책 없음 | 최대 3개 정책 선택으로 복귀 |
| 계산 불가 정책 | 필요한 값과 수정 위치 표시 |
| 구조적 제외 | 이유와 다른 후보 확인 경로 표시 |
| 공고 추출 불가 | 공식 공고 직접 확인 |
| 공고 값 누락 | 찾지 못한 필드와 공식 확인 경로 표시 |

### 19.4. 오류 문구

내부 예외 코드와 `입력 형식과 범위를 확인해 주세요` 같은 일반 문구만 보여주지 않는다. 사용자가 고칠 수 있는 필드, 원인, 다음 행동을 함께 표시한다.

---

## 20. V5 P0, P1, 제외 범위

### 20.1. P0

1. V4 복사와 독립 V5 기준선
2. V5 전용 포트, API, 자산, 세션키, 캐시
3. 큰 Hero 축소와 3개 사용자 단계
4. 해결 목적 한 개 선택
5. `매출 감소` 자동 감지 신호 전환
6. `unsure` 렌즈 제안과 사용자 확인
7. AI 검토 계획
8. 정책 후보 즉시 표시와 최대 3개 선택
9. 선택 정책의 준비 조건 전체 표시와 즉시 재계산
10. 정책 검토 순서와 기존 순위 분리
11. 검토 렌즈별 기본 초점과 지표 순서
12. 5단계 다음 확인 한 개
13. 남은 공고 필드 접기
14. 공고 최신성·캐시 상태 표시
15. 하네스 Trace와 고정 평가 세트
16. V4 원본 해시 불변과 회귀 검증

### 20.2. P1

- 검토 계획의 동의형 Luna 문장 다듬기
- 공고 변경 Diff의 사용자 화면 복귀
- 계정 없는 선택적 세션 복원
- 검토 Trace의 발표용 읽기 전용 화면
- 정책 유형별 비교 설명 개선
- 실제 사용자 테스트가 가능해진 경우 사용성 검증

P1은 P0 완료와 사용자 승인을 받은 뒤 진행한다.

### 20.3. 제외 범위

- 사용자 화면에 멀티에이전트 역할 나열
- LangGraph 전환
- 범용 금융 챗봇
- AI가 정책 순위 재계산
- AI가 자격이나 금액 결정
- AI가 정책을 자동 선택
- OCR과 문서 업로드
- 실시간 외부 공고 수집
- 자동 신청
- 은행·POS·카드 연결
- 알림 서비스
- 기관 담당자 연결
- 개인 신용평가
- 승인 가능성 예측

---

## 21. V4 복사 기반 구현 원칙

### 21.1. 복사 전 확인

새 구현 세션은 다음 순서를 지킨다.

1. 루트 `AGENT_MEMORY.md`를 읽는다.
2. 이 `V5 구현 계획표.md`를 처음부터 끝까지 읽는다.
3. `v4/README.md`, `v4/VERIFICATION.md`, `v4/main.py`, `v4/orchestrator.py`, `v4/copilot.py`, `v4/static/`, `v4/tests/test_v4.py`를 확인한다.
4. `git status --short`로 사용자 변경을 확인한다.
5. 현재 V4 소스 파일 목록과 SHA-256을 기록한다.
6. V4 회귀와 JavaScript 문법 검사를 한 번 실행한다.
7. 기준선 실패가 있으면 V5 복사 전에 원인과 영향을 보고한다.

### 21.2. 복사 대상

다음 파일과 디렉터리를 구조 그대로 `v5/`에 복사한다.

- `v4/__init__.py`
- `v4/main.py`
- `v4/orchestrator.py`
- `v4/copilot.py`
- `v4/README.md`
- `v4/static/index.html`
- `v4/static/app.js`
- `v4/static/styles.css`
- `v4/static/v3-extension.css`
- `v4/static/v4-extension.css`
- `v4/static/v4-extension.js`
- `v4/static/templates/거래내역_입력양식.csv`
- `v4/static/templates/대출_입력양식.csv`
- `v4/tests/test_v4.py`
- `v4/V2_BASELINE_SHA256.md`
- `v4/V3_COPY_BASELINE_SHA256.md`

`v4/VERIFICATION.md`는 V4의 결과 기록이므로 실행 소스와 함께 복사하지 않는다. V5에는 새 `v5/VERIFICATION.md`를 만든다.

### 21.3. 복사 제외

- `v4/__pycache__/`
- `v4/tests/__pycache__/`
- `v4/runtime/notice_extraction_cache.sqlite`
- `v4/runtime/server-v4.stdout.log`
- `v4/runtime/server-v4.stderr.log`
- 루트의 V4 서버 로그
- 임시 테스트 산출물

### 21.4. 복사 직후 증거

수정 전에 `v5/V4_COPY_BASELINE_SHA256.md`를 만든다.

문서에는 다음을 기록한다.

- 캡처 시각
- 원본 `v4/`
- 대상 `v5/`
- 제외 목록
- 대응 파일 수
- 복사 직후 일치 수
- 각 상대 경로의 V4 SHA-256
- 이름을 바꿀 파일의 원본 경로와 대상 경로

복사 직후 대응 파일 해시 일치율은 100%여야 한다.

### 21.5. 복사 뒤 이름 변경

복사 기준선을 기록한 다음 V5 안에서만 이름을 바꾼다.

- `v5/static/v4-extension.css` -> `v5/static/v5-extension.css`
- `v5/static/v4-extension.js` -> `v5/static/v5-extension.js`
- `v5/tests/test_v4.py` -> `v5/tests/test_v5.py`

`styles.css`와 `v3-extension.css`는 이름과 내용을 유지한다.

### 21.6. V5 독립 값

| 항목 | V5 값 |
| --- | --- |
| 앱 엔트리 | `v5.main:app` |
| API 버전 | `v5-api-v1.0` |
| API 접두사 | `/api/v5` |
| 기본 포트 | `8003` |
| StaticFiles 이름 | `v5-static` |
| 자산 버전 시작 | `v5-001` |
| 세션키 | `buttimaiv5:session:v1` |
| 공고 캐시 | `v5/runtime/notice_extraction_cache.sqlite` |
| 서버 표준출력 | `v5/runtime/server-v5.stdout.log` |
| 서버 오류출력 | `v5/runtime/server-v5.stderr.log` |

### 21.7. 변경 금지

- V5 구현을 위해 `v4/` 직접 수정 금지
- `v3/`와 V2 `app/` 직접 수정 금지
- 기존 CSS 일괄 교체 금지
- 공용 `src/` 편의성 변경 금지
- 기존 Rule·Event·현금·순위 계약 임의 변경 금지
- V4 공고 캐시 재사용 금지
- V4 세션키 읽기 금지

공용 엔진 차이가 필요하면 먼저 V5 어댑터로 해결한다. 공용 계약 변경이 꼭 필요하면 영향 파일, V2·V3·V4 회귀, 금융결과 변화와 대안을 제시하고 사용자 승인을 기다린다.

---

## 22. V5 파일별 변경 지도

| 파일 | V5 역할 | 계획된 변경 |
| --- | --- | --- |
| `v5/main.py` | 독립 FastAPI 앱 | V5 경로·버전·Health·Scope·캐시 분리 |
| `v5/orchestrator.py` | 검토 렌즈와 정책 준비 | 정책별 조건 목록·검토 순서·권한 불변 |
| `v5/copilot.py` | 공고 구조화 | V4 검증 유지, 필드 우선순위 메타데이터 추가 |
| `v5/static/index.html` | 3개 사용자 단계 | Hero 축소, 해결 목적, 검토 계획, 다음 확인 |
| `v5/static/app.js` | 기존 지도·입력·정책 비교 | V5 API, 검토 순서, 기본 초점, 지표 순서 |
| `v5/static/v5-extension.js` | V5 상태와 화면 연결 | 렌즈, 감지 신호, 정책별 조건 선택, 다음 확인, 세션키 |
| `v5/static/styles.css` | V2 계열 기본 디자인 | 원칙적으로 보존 |
| `v5/static/v3-extension.css` | V3 확장 디자인 | 원칙적으로 보존 |
| `v5/static/v5-extension.css` | V5 전용 Override | 축소 Hero, 렌즈, 계획 카드, 다음 확인 |
| `v5/tests/test_v5.py` | V5 전용 회귀 | 복사 기준, 권한 불변, 렌즈·정책 준비·공고 Gate |
| `v5/V4_COPY_BASELINE_SHA256.md` | V4 복사 증거 | 새로 생성 |
| `v5/VERIFICATION.md` | V5 검증 기록 | Stage별 명령, 결과, 미검증 범위 기록 |
| `v5/README.md` | 실행·범위 안내 | 실제 V5 기능과 제외 범위로 전면 갱신 |

---

## 23. 단계별 구현 계획

### Stage V5-0. V4 복사와 독립 기준선

구현:

- 현재 V4 파일 목록과 SHA-256 기록
- V4 회귀·JavaScript 기준선 확인
- 독립 `v5/` 생성과 승인된 파일 복사
- 복사 직후 대응 파일 해시 확인
- `v5/V4_COPY_BASELINE_SHA256.md` 작성
- V5 파일명, API, 포트, 자산, 세션키, 캐시 분리
- 빈 `v5/runtime/`과 새 `v5/VERIFICATION.md` 준비

Gate:

- 복사 대상 해시 일치 100%
- V4 원본 변경 0건
- `v5.main:app` 기동
- `/health` HTTP 200과 `v5-api-v1.0`
- V4와 V5 동시 실행 가능
- V4 세션키·캐시·포트 충돌 0건

### Stage V5-1. 문서 Truth와 화면 셸

구현:

- V5 README를 실제 범위로 작성
- 상단 진행을 `진단·비교·준비`로 변경
- Hero 축소
- 정상 경로에서 발표 프리셋 숨김
- `?demo=1` 프리셋 노출
- 운영 기간 제거
- 기존 지도·입력·그래프·CSS 보존

Gate:

- V5 화면 문구와 API 범위 불일치 0건
- 정상 경로 프리셋 노출 0건
- 데모 프리셋 자동 계산 0회
- 운영 기간 DOM·상태·Snapshot 잔여 참조 0건
- V4 지도와 입력 컴포넌트 핵심 DOM 유지

### Stage V5-2. 해결 목적과 AI 검토 계획

구현:

- 해결 목적 다섯 개 단일 선택
- `매출 감소` 자동 감지 신호 전환
- `unsure` 렌즈 제안과 사용자 확인
- 검토 계획 Schema와 로컬 문장
- 검토 기준 변경 기능
- 렌즈 변경 전후 권한 불변 확인

Gate:

- 해결 목적 동시 선택 최대 1개
- 렌즈 미확정 상태 자동 진행 0건
- 렌즈 변경으로 자격·금액·효과·순위가 변한 사례 0건
- 감지 신호가 사용자 목적을 자동 덮어쓴 사례 0건
- 검토 계획이 목적·재무 상태·먼저 볼 효과를 사용자 문장으로 포함

### Stage V5-3. 정책별 자격조건 선택

구현:

- 검수 질문 카탈로그 의존성 연결
- 앞단 질문 게이트 제거
- 후보별 `preparation_questions` 전체 제공
- `예`, `아니오`, `모름`, 날짜와 숫자 선택 UI
- 선택 즉시 자격 상태와 조건부 효과 재계산
- 사용자 선택과 기관 확인 항목 분리

Gate:

- 자유 생성 질문 0건
- 진단과 비교 사이 질문 UI 0개
- 정책별 표시 필드와 공용 `POLICY_FIELDS` 불일치 0건
- 조건 선택 뒤 준비 화면 이탈 0건
- 이전 화면으로 보내는 `답변 수정` 버튼 0개
- 외부 AI 없이 정책 조건 선택과 재계산 완주 100%

### Stage V5-4. 검토 순서와 정책 비교

구현:

- `rank_position`과 `review_position` 분리
- 렌즈별 금융 메커니즘 우선순위
- 검토 1번 정책 기본 초점
- 검토 이유 문구
- 렌즈별 지표 순서
- 기존 그래프와 조건부 점선 유지
- 반복 고지 통합

Gate:

- 렌즈 변경 전후 정책 후보 ID 집합 일치 100%
- 렌즈 변경 전후 자격·금액·효과·순위 일치 100%
- 고정 평가 케이스의 검토 순서 일치 100%
- 기본 초점과 검토 1번 정책 일치 100%
- 조건부 정책의 순위 포함 0건
- 검토 순서를 추천 순위로 표현한 문구 0건

### Stage V5-5. 다음 공고 확인

구현:

- V4 공고 전체 구조화와 캐시 이식
- V5 캐시 DB 분리
- 여섯 필드 유지
- 다음 확인 한 개 우선 표시
- 렌즈별 공고 필드 순서
- 남은 필드 접기
- 누락·검증 실패 필드 묶기
- 저장 기준일·캐시 상태·공식 링크 표시
- 확인 완료 후 다음 항목 자동 승격

Gate:

- V4 공고 원문 검증 로직 보존
- 사용자 데이터 외부 전송 0건
- 근거 없는 필드 표시 0건
- 검증 실패 필드 자동 확정 0건
- 공고 해시 변경 뒤 과거 확인 재사용 0건
- 첫 화면에 펼쳐진 확인 항목 최대 1개
- V4 캐시 DB 읽기·쓰기 0건

### Stage V5-6. 하네스와 AI 평가

구현:

- 엄격한 상태 Schema
- 도구 허용 입력과 출력
- 금융 권한 불변 해시
- 정책 준비·검토 Trace
- 시간초과·Fallback·Fail-close
- 고정 Oracle 케이스
- 정책별 준비 조건 계약 대조

Gate:

- Schema 밖 출력의 상태 반영 0건
- AI 도구가 금융결과를 변경한 사례 0건
- 외부 AI 실패 시 진단·비교 완주 100%
- 공고 숫자·날짜 근거 불일치 표시 0건
- 진단과 비교 사이 질문 수 0개
- 모든 Fallback에 사용자 다음 행동 존재

### Stage V5-7. 회귀·접근성·발표 검증

구현:

- V5 전용 회귀 전체
- V4 전용 회귀 재실행
- 관련 공용 정책 통합 회귀
- JavaScript 문법 검사
- API Health·Scope·404 계약
- 데모 모드 대표 시나리오
- 키보드와 포커스 상태
- 모바일 DOM·CSS 규칙
- 발표용 Trace와 AI 역할 설명

Gate:

- V5 전용 테스트 전부 통과
- V4 회귀 전부 통과
- 관련 공용 회귀 전부 통과
- V4 복사 기준 핵심 파일 해시 불변
- JavaScript 문법 2개 파일 통과
- Git 공백 검사 통과
- 금지 금융 표현 0건
- 고정 페르소나 기대 행동 8/8
- 최종 시각·모바일 체감·발표 리허설은 사용자 승인

---

## 24. 평가 세트

### 24.1. 검토 렌즈 평가

`reports/v5/evaluation/review_lens_cases.json`에 25개 고정 케이스를 만든다.

구성:

- 해결 목적 5개
- 현금 부족, 대출 부담, 고정비 부담, 정책 유형 혼합, 신호 불명확 사례
- 정책 후보 0개, 1개, 2개, 3개
- 조건부·구조적 제외·계산 가능 정책 조합

각 케이스의 Oracle:

- 확정 검토 렌즈
- 감지 신호
- 기대 검토 1번 정책 또는 없음
- 기대 지표 순서
- 자격·금액·효과·순위 불변 해시

### 24.2. 정책별 준비 조건 평가

각 API 후보의 `preparation_questions`를 공용 `POLICY_FIELDS`와 대조한다.

검증 항목:

- 후보 정책의 전체 검수 필드 순서
- 각 선택 항목의 정책 ID 범위
- 앞단 질문과 건너뛰기 UI 부재
- 사용자 선택 뒤 자격과 조건부 그래프 재계산
- 기관 확인 항목의 별도 표시

기존 `reports/v5/evaluation/question_planner_cases.json` 30개는 앞단 질문 게이트 설계의 이력으로 보존하며 현재 성공 기준에는 사용하지 않는다.

### 24.3. 공고 추출 평가

V4의 공고 추출 테스트를 복사하고 다음을 추가한다.

- V5 캐시 경로 분리
- 렌즈별 필드 우선순위
- 신청 기간 누락 시 직접 확인 우선
- 숫자 근거 불일치 필드 Fail-close
- 공고 해시 변경 시 확인 상태 분리
- 최대 3개 정책의 독립 분석 상태

### 24.4. 사용자 흐름 평가

고정 페르소나 8개에서 다음을 기록한다.

- 해결 목적 선택
- 첫 실행 가능한 결과까지 클릭 수
- 준비 화면의 정책별 조건 수
- 비교 화면 도달 여부
- 기본 초점 정책과 이유
- 다음 확인 항목
- 금지 표현 여부

이는 기능 일관성 평가다. 실제 사용자의 만족도, 이해도 또는 실제 정책 효과로 표현하지 않는다.

### 24.5. 성공 기준

| 지표 | 목표 |
| --- | --- |
| 해결 목적 때문에 자격·금액·효과·순위가 변한 사례 | 0건 |
| Oracle 검토 순서 일치 | 25/25 |
| 정책별 준비 조건 계약 일치 | 100% |
| 진단과 비교 사이 질문 수 | 0개 |
| 선택 뒤 같은 준비 화면 유지 | 100% |
| 공고 숫자·날짜 근거 불일치 표시 | 0건 |
| 외부 AI 장애 시 핵심 경로 완주 | 100% |
| 고정 페르소나 기능 일관성 | 8/8 |
| 금지 금융 표현 | 0건 |
| 첫 실행 가능한 결과까지 시간 | 발표 시나리오 기준 3분 이내, 사용자 리허설 확인 |

---

## 25. 대표 발표 시나리오

### 25.1. 시작

`/?demo=1`로 진입해 대출 상환 부담이 큰 고정 페르소나를 불러온다.

사용자는 `대출 상환 부담 줄이기`를 직접 선택한다.

### 25.2. 진단

입력 원장에서 매출, 현금, 비용, 대출을 확인하고 진단을 실행한다.

V5는 다음을 보여준다.

- 가게 매출 방향
- 상권 시나리오
- 28일 필요현금 상태
- 13주 현금 기준선
- 대출 관련 감지 신호

### 25.3. AI 검토 계획

화면은 대출 부담을 먼저 보기로 했다는 점과 대환 정책을 첫 비교로 배치할 이유를 설명한다.

진단과 비교 사이에는 질문을 두지 않는다. 정책을 선택해 비교한 뒤 준비 화면에서 대환대출에 필요한 전체 검수 조건을 직접 고른다.

### 25.4. 정책 비교

사용자가 선택한 정책 최대 3개를 무대응과 비교한다.

- 대환대출을 검토 1번으로 연다.
- 월 최대상환 변화, 6개월 부채 변화, 신규 부채 변화를 먼저 보여준다.
- 재도전특별자금과 바우처도 같은 계산값으로 확인할 수 있다.
- 조건부 정책은 점선이며 순위에 들어가지 않는다.

### 25.5. 다음 확인

대환대출 준비 화면에서 공고 구조화 결과를 불러온다.

신청 기간과 현재성을 먼저 확인하고, 다음으로 대환 한도·금리·기간 조건을 확인한다. 공고에서 찾지 못했거나 검증에 실패한 값은 공식 공고 직접 확인으로 닫는다.

### 25.6. 발표에서 설명할 AI 역할

- LightGBM: 상권 단위 하방·기준·회복 시나리오
- 정책 준비 도구: 선택한 정책의 검수 조건을 준비 화면에서 직접 선택
- 검토 렌즈: 사용자 목적에 맞춰 정책과 지표 검토 순서 조정
- Luna: 공개 공고 전체에서 신청 핵심정보 구조화
- 근거 검증 하네스: Chunk·인용·숫자 검증과 필드별 Fail-close
- 결정론 엔진: 자격·금액·효과·순위의 최종 권한

멀티에이전트나 LangGraph를 사용하지 않았다는 점을 약점처럼 설명하지 않는다. 필요한 역할을 제한된 도구와 검증 계약으로 통제했다는 점을 보여준다.

---

## 26. 금지 표현

서비스, 발표, 문서에서 다음 표현을 사용하지 않는다.

- AI가 가장 좋은 정책을 추천합니다
- 승인 가능성이 높습니다
- 이 정책을 받게 됩니다
- 폐업을 예측합니다
- 개인 매출을 예측합니다
- 정책 효과가 보장됩니다
- 실시간 최신 공고입니다
- 신청 자격이 확정되었습니다
- AI가 금융 결정을 내립니다
- 여러 에이전트가 토론해 최적 답을 찾았습니다

권장 표현:

- 입력한 답변 기준으로 확인됩니다
- 공식 확인이 더 필요합니다
- 조건부 계산입니다
- 무대응 대비 계산 결과입니다
- 검토 순서를 먼저 배치했습니다
- 저장 공고 기준으로 구조화했습니다
- 공식 공고와 신청기관에서 최종 확인해야 합니다

---

## 27. 구현 중 사용자 승인이 필요한 경우

이 문서에 적힌 P0 범위와 V4 복사 원칙은 승인된 구현 계약으로 본다. 다음 상황이 새로 생기면 구현을 멈추고 사용자 승인을 받는다.

- 새로운 자격 Rule 또는 임계값 필요
- 기존 금융 계산식 변경
- 정책 금액·효과·순위 변경
- 새로운 공고 구조화 필드 추가
- 사용자 재무 외부 AI 전송
- 사용자 데이터 장기 저장
- 공용 `src/` 계약 변경
- V4 원본 수정 필요
- 새 외부 데이터 필요
- 멀티에이전트 또는 LangGraph 도입
- OCR·문서 업로드·기관 전송·자동 신청 추가
- 기존 5단계 내부 상태를 완전히 폐기하는 재구축

다음은 통상적인 구현 세부사항이므로 별도 승인을 반복하지 않는다.

- V5 내부 함수 분리
- V5 전용 CSS 클래스명
- 테스트 Fixture 정리
- 접근성 속성 보완
- V5 전용 오류 문구 수정
- V5 전용 캐시 Migration이 아닌 신규 생성
- 동일 계약 안의 코드 정리

---

## 28. 구현 완료 기준

V5는 다음 조건을 모두 만족할 때 코드 Gate 완료로 판단한다.

1. V4 복사 기반의 독립 `v5/` 서비스다.
2. V4 원본과 V4 캐시를 수정하지 않는다.
3. 정상 경로는 `진단·비교·준비`로 보인다.
4. 사용자는 해결 목적 한 개를 선택한다.
5. AI 검토 계획이 목적과 재무 신호를 연결한다.
6. 진단과 비교 사이에 별도 질문을 두지 않는다.
7. 선택한 정책의 전체 검수 조건을 준비 화면에서 직접 선택하고 즉시 다시 계산한다.
8. 정책 검토 순서와 기존 순위가 분리된다.
9. 해결 목적이 자격·금액·효과·순위를 바꾸지 않는다.
10. 검토 1번 정책과 이유가 첫 비교에 보인다.
11. 13주 현금과 6개월 부채 비교가 유지된다.
12. 조건부 정책은 점선이며 순위에서 제외된다.
13. 5단계는 다음 공고 확인 한 개를 우선 보여준다.
14. 공고 구조화 값은 원문 근거 검증을 통과해야 한다.
15. 공고 누락·검증 실패는 필드별 직접 확인으로 닫힌다.
16. 사용자 재무정보가 외부 AI와 영구 캐시에 들어가지 않는다.
17. 외부 AI가 실패해도 진단과 비교를 완주한다.
18. V5, V4, 관련 공용 회귀가 모두 통과한다.
19. 고정 평가 세트가 목표를 충족한다.
20. 최종 화면과 발표 흐름은 사용자가 승인한다.

---

## 29. 새 구현 세션 시작 순서

새 세션에서는 다음 순서로 바로 시작한다.

1. `AGENT_MEMORY.md`와 이 문서를 읽는다.
2. `git status --short`로 사용자 변경을 확인한다.
3. `v4/` 현재 파일과 V4 검증 상태를 확인한다.
4. Stage V5-0만 먼저 수행한다.
5. `v5/V4_COPY_BASELINE_SHA256.md`와 독립 실행을 검증한다.
6. V4 원본 해시 불변을 확인한다.
7. Stage V5-1부터 순서대로 구현한다.
8. 각 Stage Gate를 통과한 뒤 실제 결과가 다음 Stage에 미치는 영향을 검토한다.
9. 결과 의미나 범위를 바꾸는 새 선택이 생기면 사용자에게 보고하고 기다린다.
10. 코드 Gate가 끝나도 시각 승인과 발표 리허설을 자동 완료로 처리하지 않는다.

새 세션에 전달할 첫 요청 예시:

> `V5 구현 계획표.md`와 루트 `AGENT_MEMORY.md`를 읽고 Stage V5-0부터 시작해줘. 현재 `v4/`의 승인된 실행 소스와 정적 자산을 독립 `v5/`로 복사하고, 복사 직후 SHA-256 일치와 V4 원본 불변을 확인한 뒤 V5 안에서만 수정해. 각 Stage Gate와 결과 피드백 검토를 지키고, 결과 의미·범위·권한 경계를 바꾸는 선택이 생기면 실행 전에 멈춰서 알려줘.

---

## 30. 현재 상태

2026-08-26 기준으로 Stage V5-0부터 V5-7까지 코드와 비시각 Gate를 완료했다.

- V4 승인 파일 16개를 독립 `v5/`로 복사하고 복사 직후 SHA-256 16/16 일치를 기록했다.
- V5 API, 정적 자산, 세션키, 포트 8003과 공고 캐시 경로를 분리했다.
- 한 개 해결 목적, AI 검토 계획, 정책 검토 순서와 기존 순위 분리, 정책별 준비 조건 선택, 다음 공고 확인 1개를 구현했다.
- 사용자 화면 피드백에 따라 내부 신호명과 지원방식 키를 한국어로 바꾸고, 내부 설계 설명 카드와 앞단 질문 게이트를 제거했다.
- 정책 후보를 바로 보여주며, 선택 정책의 준비 화면에서 그 정책의 검수 조건 전체를 직접 선택하고 같은 화면에서 즉시 다시 계산한다.
- 공고 분석 중 회전 표시를 추가하고 공고 조각 수·원문 해시는 숨겼다. 확인 수는 정책 수가 아니라 선택 정책 한 건의 신청정보 여섯 항목임을 명시하고, 완료 뒤 공식 접수 확인과 비교 복귀 행동을 제공한다.
- 검토 렌즈 25/25와 고정 페르소나 8/8 Oracle을 통과했고 정책별 준비 조건이 공용 계약과 일치함을 확인했다. 기존 질문 계획 30개는 이전 설계 이력으로만 보존한다.
- V5 전용 회귀 17/17과 V4·관련 공용 정책 회귀 83/83을 통과했으며, 사용자 피드백 뒤 V3·V4 집중 회귀 30/30도 통과했다.
- JavaScript 2개와 Python 소스·테스트 문법 검사를 통과했다.
- V4 원본은 복사 기준 SHA-256 16/16을 유지하며 공용 `src/`도 수정하지 않았다.
- V5 서버는 `127.0.0.1:8003`에서 실행하며 `/health`가 `v5-api-v1.0`을 반환한다. 실제 PID는 `v5/VERIFICATION.md`에 기록한다.
- 외부 AI 호출, 외부 데이터 획득, 사용자 재무정보 외부 전송, 배포는 수행하지 않았다.

상세 결과는 `v5/VERIFICATION.md`에 기록했다. 최종 화면 시각 승인, 모바일 체감 검수와 발표 3분 리허설은 사용자 확인이 남아 있다.

---

## 31. V5 Final 마무리 결정

2026-08-27 사용자는 공개자료 기반 실제 수요 조사 결과를 검토한 뒤 V6를 새로 만들지 않고 V5를 최종 제출본으로 마무리하기로 결정했다. 이 결정은 실제 소상공인 실험이 없는 대학생 AI 서비스 제작 프로젝트의 검증 범위에 맞춘 종료 결정이다.

남은 작업은 다음 여섯 항목으로 제한한다.

1. 첫 화면과 발표 문구에서 목표 사용자를 `현금 부족과 기존 부채 사이에서 정책금융 실행을 고민하는 소상공인`으로 분명히 표시한다.
2. 핵심 설명을 `추가 대출이 위기를 해결하는지 부채만 늘리는지 계산하는 서비스`로 통일한다.
3. 대표 데모 한 건에서 무대응, 비차입 지원, 신규 정책자금의 13주 현금과 6개월 부채 차이를 보여준다.
4. 저장 공고의 기준시점, 조건부 계산, 공식기관 확인, 실제 승인 여부의 한계를 눈에 띄게 유지한다.
5. 사용자가 데스크톱·모바일 핵심 흐름과 발표 리허설을 최종 확인하고, 관련 회귀와 링크 검사를 한 번 수행한다.
6. README, 기능명세서, 발표자료와 포트폴리오에서 조사 근거, 8개 고정 가상 시나리오의 의미, 실제 사용자 검증 부재를 같은 표현으로 설명한다.

V5 Final에서는 새 모델, 새 외부 데이터, V6 기능, 새 주제 MVP, 계좌·카드·POS 연동, 실제 신청 자동화, 장기 사용자 계정과 알림을 추가하지 않는다. 위 작업을 마치면 V5를 최종본으로 동결하고 현재 프로젝트를 종료한다.
