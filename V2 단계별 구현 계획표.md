# V2 단계별 구현 계획표

> 상태: V2-011 현행 구현·사용자 경험·기능 추적 기준 문서  
> 기준일: 2026-08-23  
> 이전 문서: `MVP 단계별 구현 체크리스트.md`는 MVP 구현 이력으로 그대로 보존한다.

## 0. V2 서비스 명명 계약

- 공식 서비스명: **버팀AI — 소상공인 13주 현금 생존 코파일럿**
- 화면 상단 축약명: **버팀AI**
- 브라우저 제목·V2 문서에서는 공식 서비스명을 사용하고, 공간이 제한된 헤더에서는 축약명을 사용한다.
- 한글 음차인 `버팀아이`는 사용하지 않는다.
- 기존 `내일상권 AI 금융 코파일럿`은 V2 화면과 V2 기준 문서에서 더 이상 사용하지 않는다.

## 1. V2 목표

V2는 단순한 현금흐름 계산기와 정책 챗봇의 결합이 아니라, 사용자의 실제 점포 상황에서 **진단 → 정책 탐색 → 필요한 조건만 확인 → 금액·시점이 확정된 대안 비교 → 다음 행동 정리**까지 이어지는 제한형 AI 금융 의사결정 서비스다.

- 개별 점포의 폐업·승인 가능성을 예측하지 않는다.
- LightGBM은 상권·업종 집계 전망의 하방·기준·회복 범위만 제공한다.
- Hybrid Search는 점포 상황과 가까운 금융정책 후보를 내부적으로 최대 6개 찾고, 화면에는 우선 후보 최대 3개를 보여준다.
- 규칙 엔진은 검색된 후보군에 필요한 미확인 조건을 질문하고 적격·부적격·추가 확인 상태를 구분한다. 현재 질문은 후보 맞춤형이지만 이전 답변에 따라 다음 질문을 동적으로 생략하는 완전 적응형 구조는 아니다.
- 현금흐름·정책 Event·추천 엔진은 사용자 확정값을 계산하며, 별도로 검수된 조건부 금액·일정 가정은 점선 효과로만 계산하고 추천 순위에서는 제외한다.
- LLM은 계산·자격·순위를 바꾸지 않고, 확정된 계산과 공식 근거를 읽기 쉬운 행동 브리프로 정리한다.

## 2. MVP 보존 계약

다음 자산은 V2에서도 삭제하거나 대체하지 않고 재사용한다.

| 자산 | 보존 기준 | 회귀 확인 |
|---|---|---|
| 서울 상권 지도 | 자치구 → 행정동 → 개별 상권 선택, 검색·목록·지도 동기화 | 지도 레벨 이동, 원·라벨, 선택값 유지 |
| 발표용 예시 입력 | 5개 점포 상황과 6개월 매출·재무 자동 입력 | 모든 필드와 V2 신규 기타 고정비 입력 |
| 매출 입력 | 기본 6개월, 최소 3개월, 최대 12개월 | 추가·삭제·요약 갱신 |
| 현금 그래프 | 3개 상권 전망 범위의 13주 비교 | 범례·선 클릭, 포인터 호버, 리사이즈 |
| 대안 그래프 | 확정된 모든 대안의 13주 비교 | 범례·카드·선 선택 동기화 |
| 로딩 화면 | 처리 문구, 경과시간, 중첩 호출 안전성 | 성공·실패 후 반드시 닫힘 |
| CSV 간편 입력 | 원본 비저장, UTF-8 입력, 현금 진단 | 기존 API와 양식 링크 유지 |
| 접근성·반응형 | 키보드 선택, aria-live, 다이얼로그, 모바일 배치 | 기존 자동 검사와 수동 화면 확인 |

보존 대상의 DOM ID와 이벤트 계약은 특별한 이유가 없으면 바꾸지 않는다. V2 신규 UI는 기존 요소를 감싸거나 사이에 추가하는 방식으로 구현한다.

## 3. V2 사용자 서비스 이용 경험

이 절은 단순한 기능 목록이 아니라, **처음 접속한 사용자가 어떤 화면을 보고, 무엇을 입력하고, 서비스가 어떻게 반응하며, 어떤 결과로 이동하는지**를 정의한 화면 설계 기준이다.

### 3.1. 전체 경험 한 눈에 보기

> 사용자에게 보이는 핵심 흐름: **가게 선택 → 매출·지출·대출 입력 → 13주 현금 진단 → 질문 한 개씩 응답 → 답변을 반영한 정책 최대 3개 확인·선택 → 신청 준비와 조건부 현금효과 확인 → 확정 대안 비교 → 다음 행동 확인**

| 단계 | 사용자의 핵심 질문 | 화면에서 바로 보여줄 답 | 사용자의 주요 행동 |
|---|---|---|---|
| 1. 사업장 선택 | 내 가게는 어떤 상권·업종에 속하나? | 선택한 상권, 업종, 운영 기간 | 지도·검색·목록으로 사업장 지정 |
| 2. 재무 입력 | 우리 가게에 돈이 얼마나 들어오고 나가나? | 매출만이 아닌 현금·네 가지 비용·대출 전체 입력 | 최근 매출, 현재 현금, 지출, 대출 입력 |
| 3. 진단·정책 확인 | 지금 현금은 버틸 수 있고, 어떤 정책을 확인해야 하나? | 점포 추세, 상권 전망, 13주 현금, 단일 질문, 답변 반영 정책 최대 3개 | 전망 범위 선택, 질문 응답, 정책 확인·선택 |
| 4. 비교·다음 행동 | 왜 지금 안 되고, 어떻게 준비하며, 현금은 얼마나 달라질 수 있나? | 정책별 원 질문·실제 답변·해결 행동, 조건부 효과, 무대응과 확정 대안 비교 | 문제 답변 수정, 공식 공고 확인, 대안·상담 선택 |

#### 사용자 화면 순서와 내부 처리 순서의 차이

사용자 화면은 질문 우선이지만 서버는 질문 목록을 만들기 위해 첫 현금 진단 요청에서 정책 검색을 먼저 수행한다.

1. 사용자가 `현금 진단 보기`를 누르면 서버가 현금 계산, 상권 시나리오, 1차 정책 검색과 자격 평가를 함께 수행한다.
2. 클라이언트는 1차 정책 후보를 바로 보여주지 않고, 그 후보군에 필요한 미확인 질문만 한 장씩 표시한다.
3. 사용자가 마지막 질문에 답하면 같은 비교 API를 답변과 함께 다시 호출한다.
4. 두 번째 응답의 정책 후보를 화면에 최대 3개 표시하고, 우선순위 상위 2개는 최초 1회 자동 선택한다. 사용자는 선택을 해제하거나 최대 3개까지 바꿀 수 있다.
5. 4단계로 이동할 때 선택 정책 중 조건부 그래프가 허용된 정책을 다시 계산한다.

따라서 현행 구조를 `질문 자체가 AI로 실시간 생성되는 구조` 또는 `답변마다 다음 질문을 AI가 재결정하는 구조`로 설명하면 안 된다. 정확한 표현은 **상황 검색으로 후보군을 정한 뒤, 검수된 후보별 질문을 한 장씩 제시하는 단계형 질문 흐름**이다.

### 3.2. 접속 직후: 서비스를 이해하는 첫 10초

사용자는 첫 화면 상단에서 `내 가게 현금이 얼마나 버티는지 보고, 확인할 금융정책을 바로 비교한다`는 핵심 효익을 먼저 본다. 그 아래에서 긴 설문보다 현금 진단이 먼저라는 점과, 폐업·대출 승인·확정 자격은 예측하지 않는다는 경계를 확인한다.

- 상단 진행표시는 `1 사업장 선택 → 2 재무 입력 → 3 진단·정책 확인 → 4 비교·다음 행동`의 네 단계만 보여준다.
- 좌측 또는 상단의 `현재 입력` 요약은 선택한 사업장·업종·매출·현금·상권 범위를 지속적으로 보여준다.
- AI 기술명을 첫 화면의 주인공으로 두지 않는다. 사용자가 먼저 보아야 할 것은 `내 현금 위험·비교할 정책·다음 행동`이다.

### 3.3. 입력 경로: 직접 입력과 발표용 빠른 시연

첫 화면에서 사용자는 두 경로 중 하나로 시작할 수 있다.

#### A. 일반 사용자의 직접 입력

1. 서울 지도에서 `자치구 → 행정동 → 개별 상권` 순으로 범위를 좁힌다.
2. 지도 대신 상권명 검색이나 목록을 사용해도 되며, 어떤 방법을 쓰든 지도·목록·선택 요약은 같은 상권으로 동기화된다.
3. 업종 대분류와 세부 업종, 운영 기간을 선택한다.
4. `재무 입력하기`를 누르면 사업장 선택을 검증하고 2단계로 이동한다. 누락 시 화면을 넘기지 않고 정확한 필수 항목을 알린다.

#### B. 발표용 빠른 시연

1. `발표용 빠른 시연`을 펼치면 현금 여유·안정 운영·매출 하락·고정비 부담·대출 상환 부담의 다섯 상황을 본다.
2. 하나를 고르면 서로 다른 예시 상권과 6개월 매출, 현재 현금, 임대료, 인건비, 필수 매입비, 기타 고정비, 대출 값이 자동으로 채워진다.
3. 예시를 고른 행위만으로 진단이 시작되지는 않는다. 사용자는 채워진 값을 2단계에서 확인한 뒤 `현금 진단 보기`를 눌러야 한다.
4. 따라서 빠른 시연은 결과를 자동 재생하는 동영상이 아니라, 입력 시간만 줄여주는 체험 장치다.

### 3.4. 1단계 — 사업장 선택

#### 사용자가 보는 것

- 서비스의 핵심 효익과 예측하지 않는 범위
- 서울 상권 검색창, 자치구·행정동·상권 목록, 상호작용 지도
- 선택한 상권 이름과 업종·운영 기간 입력
- 발표용 상황 5개를 불러오는 접을 수 있는 영역

#### 사용자가 하는 일

- 상권을 지정하고 업종·운영 기간을 선택한다.
- 좌측 `현재 입력`에 사업장과 업종이 올바르게 표시되는지 확인한다.
- `재무 입력하기`를 누른다.

#### 서비스의 반응

- 선택 결과를 지도·목록·입력 요약에 동시 반영한다.
- 상권 또는 업종이 비어 있으면 다음 단계로 넘어가지 않고 누락 항목을 안내한다.
- 이 단계에서는 AI 결과나 정책 추천을 미리 보여주지 않는다.

### 3.5. 2단계 — 매출·지출·대출 입력

#### 사용자가 보는 것

- 기본 6개월, 최소 3개월, 최대 12개월의 최근 월매출 입력칸
- 매출 유입 시기와 비용 지출 시기
- 현재 보유 현금
- 월 임대료·인건비·필수 매입비·기타 고정비의 네 가지 지출 항목
- 남은 대출 잔액·이자율·상환 방식·잔존 기간
- 선택 사항으로, 실제 입출금·대출 CSV를 사용하는 상세 입력 경로

#### 사용자가 하는 일

1. 최근 매출을 `가장 최근 달 → 과거 달` 순으로 입력한다.
2. 현재 현금과 네 가지 월 비용을 입력한다. 매출 데이터만 입력하는 서비스가 아니며, 이 비용과 대출 상환이 현금 진단에 함께 들어간다.
3. 대출이 없으면 잔액 0원을 입력하고, 있으면 이자율·상환 조건을 추가로 입력한다.
4. `현금 진단 보기`를 누른다.

#### 서비스의 반응

1. 필수 값과 음수 여부를 검증한다. 누락·오류가 있으면 입력 화면을 유지하고 수정할 항목을 알린다.
2. 검증을 통과하면 전체 화면 중앙에 로딩창을 열고 `세 가지 매출 변화 범위의 현금흐름을 계산하고 있습니다`와 실제 경과 시간을 표시한다.
3. 로딩 동안 상권·업종 집계 전망을 불러오고, 하방·기준·회복 세 범위의 13주 현금을 동일한 입력값으로 계산한다.
4. 계산이 완료되면 로딩창을 닫고 3단계 결과 영역으로 이동한다. 실패하면 로딩창을 반드시 닫고 이유를 안내하며, 입력값은 유지한다.

### 3.6. 3단계 — 현금 진단과 정책 경로 선택

3단계는 결과만 보는 화면이 아니라, **현금 위험을 이해한 뒤 비교할 정책을 확정하는 작업 화면**이다.

#### 3.6.1. 전망 범위를 고른다

- 사용자는 하방·기준·회복 세 장의 카드에서 13주·6개월 매출 변화율을 동시에 본다.
- 세 범위는 어느 하나가 `진짜 미래`라는 예측이 아니라, 선택한 상권·업종 집계 데이터에서 계산한 시나리오 범위다.
- 기준 범위가 기본이지만, 사용자가 카드·그래프 선·범례 중 어느 곳을 눌러도 범위가 바뀌고 선택 상태가 동기화된다.
- 선택한 범위는 3단계 현금 진단과 4단계의 모든 대안에 같이 적용된다.

#### 3.6.2. `내 점포`와 `상권 AI`를 섞지 않고 본다

사용자는 다음 두 신호를 별도의 카드로 본다.

- `내 점포 최근 흐름`: 사용자가 입력한 가장 최근 매출과 가장 오래된 매출의 차이다.
- `상권·업종 AI 전망`: LightGBM이 제시한 선택 상권·업종 전체의 전년 동기 대비 범위다.

따라서 `내 매출이 줄었다`와 `상권 전체의 하방 가능성이 있다`는 서로 다른 정보로 전달된다. AI가 내 점포 매출을 직접 예측한 것처럼 보이면 안 된다.

#### 3.6.3. 13주 현금 위험을 읽는다

- 핵심 진단 숫자와 13주 현금 그래프, 6개월 요약을 본다.
- 그래프의 노란 영역은 `현금은 남지만 앞으로 28일 내 필수 비용과 대출 상환에 부족한 구간`을 뜻한다.
- 0원 아래는 별도의 `현금 적자 구간`으로 읽는다.
- `앞으로 28일 필요현금`을 누르면 계산 의미와 그래프 영역 설명을 다이얼로 확인한다.
- 그래프 선·범례 호버로 값을 탐색하고, 클릭으로 적용할 범위를 바꾼다.

#### 3.6.4. 정책을 보여주기 전에 한 질문씩 답한다

V2-007부터 사용자 화면의 순서를 `정책 카드 → 질문 목록`이 아니라 `단일 질문 → 답변 반영 검색 → 정책 결과`로 바꾼다. 질문은 3단계 안의 전환형 화면으로 제공하며 팝업이나 별도 최상위 단계를 추가하지 않는다.

- 한 화면에는 질문 한 개만 표시한다.
- 예·아니오·모름 질문은 `아니오 / 모름 / 예` 순서의 세 버튼으로 제공한다.
- 선택형 질문은 드롭다운 대신 한 장 안의 선택 버튼으로 제공한다.
- 날짜·숫자는 같은 카드에서 입력하고 `모름`을 선택할 수 있다.
- 버튼을 선택하면 즉시 다음 질문으로 이동하며, `이전 질문`으로 돌아가 답변을 수정할 수 있다.
- 질문의 이유와 답변 영향을 같은 카드에 표시한다.
- `모름`은 부적격이나 자동 제외로 처리하지 않는다.

현재 V2-007은 기존 검수 질문 Catalog를 한 장씩 보여주는 질문 기능과 마지막 답변 이후의 기존 정책 검색 연결까지 구현한다. 초기 진단 응답은 질문 Catalog를 구성하기 위해 기존 Discovery를 내부적으로 사용하지만 정책 결과는 숨긴다. 질문 순서는 여러 후보에 공통으로 쓰이는 항목과 입력 형식을 기준으로 정렬할 뿐, 답변 직후 서버를 재호출해 불필요한 후속 질문을 제거하지는 않는다. 완전한 사전질문 전용 API와 답변 기반 분기는 구현되지 않았다.

#### 3.6.5. 마지막 답변 뒤 기존 정책 결과로 연결한다

- 마지막 질문에 답하면 현재 답변을 포함해 비교 API를 다시 실행한다.
- 재검색이 끝나면 기존 정책 결과 카드, 상황 연결 이유, 자격·접수 상태, 공식 공고와 최대 3개 선택 기능을 표시한다. 최초 결과에서는 상위 2개가 자동 선택되며 사용자가 이를 바꿀 수 있다.
- V2-007에서는 정책 결과 이후의 금액·날짜 입력, 비용 절감, 재계산, 4단계 비교 기능을 기존 구현으로 연결했다. 이후 V2-008 시험 구현에서는 이 입력을 기본 흐름 밖의 선택 기능으로 옮겼으며 사용자 검토 전에는 확정 계약이 아니다.
- 사용자는 결과 화면의 전체 `답변 수정`으로 처음부터 검토할 수 있다. 4단계 정책 카드에서는 실패하거나 미확인인 질문의 `이 답변 수정`만 눌러 해당 질문 하나를 고친 뒤 재계산된 같은 정책 카드로 돌아온다.

#### 3.6.6. V2-008 시험 구현: 미확정 값은 기본 흐름에서 요구하지 않는다

이 절은 사용자 사용성 검토를 위한 시험 구현이며 아직 확정하지 않았다. 서비스는 지원금·지급일·절감액을 추정해서 넣지 않고, 사용자가 이를 모르는 상태에서도 정책과 다음 행동까지 진행할 수 있게 한다.

- 기존 `앞으로 28일 필요현금 - 13주 최저 현금`의 양수 차액을 `13주 추가 필요 현금`으로 표시한다. 새 대출 권장액이나 승인금액이 아니다.
- 정책 카드에는 검수된 공식 지원 형태·상한과 추가 필요 현금을 나란히 안내하지만, 실제 인정액·지급일을 알기 전에는 정책 현금 유입으로 계산하지 않는다.
- 기본 정책 결과에는 금액·날짜 입력판과 항목별 비용 절감 입력판을 노출하지 않는다.
- 이미 구체적인 신청 계획이 있는 사용자는 정책 카드의 `상세 현금 영향 계산`을 열어 기존 금액·날짜 Event 입력을 사용할 수 있다.
- 이미 정한 절감 계획이 있는 사용자는 `절감 계획이 이미 있습니다`를 열어 기존 항목별 절감 대안을 계산할 수 있다.
- 사용자가 실제 금액·날짜를 입력한 검수 Event는 확정 입력 대안으로 계산한다.
- 실제 금액·날짜가 없어도 V2-011에서 승인한 네 정책의 검수된 조건부 가정은 보완 가능·공식 확인 필요 상태일 때 점선 그래프로 계산한다. 구조적 제외조건이 있으면 서버 단계에서 그래프 생성을 차단한다.
- 그 밖의 정책은 다음 행동 후보로 유지하되 검수된 계산 가정이 없으면 현금 그래프와 숫자 순위에서 제외하고 미산정 이유를 표시한다.
- 임의의 고정 5% 절감과 서비스가 추정한 지원금·지급일은 계속 사용하지 않는다.

선택형 상세 계산에서 정책 금액·날짜나 비용 절감액을 바꾸면 사용자는 `선택한 조건으로 다시 계산`을 눌러야 한다. 서비스는 바뀐 값을 즉시 순위에 반영하지 않는다.

### 3.7. 4단계 — 확정 대안 비교와 다음 행동

#### 3.7.1. 첫 진입 시 보는 순서

1. **적용 중인 상권 범위**: 3단계에서 고른 하방·기준·회복 범위와 13주 변화율을 확인한다. `범위 변경`을 누르면 3단계 선택 영역으로 돌아간다.
2. **로컬 행동 브리프**: 현재 기준 결과의 13주·6개월 현금, 신규 부채, 가장 먼저 확인할 한 가지를 바로 본다. 외부 LLM을 사용하지 않아도 이 내용은 항상 제공된다.
3. **선택 정책의 신청 준비**: 구조적 제외, 보완 가능한 조건, 공식 확인 필요를 구분하고 `원 질문·사용자 선택 답변·결과·해결 행동`을 함께 보여준다. 수정은 실패·미확인인 사용자 답변에만 제공하고 접수·예산 같은 공식 확인값은 편집하지 않는다.
4. **확정·조건부 현금 비교**: 보완 가능 또는 공식 확인 필요 정책에 검수된 금액·기간 가정이 있을 때만 무대응 대비 조건부 현금효과를 자동 계산한다. 업종·기업규모·소재지·중복수혜 등 구조적 제외조건이 있는 정책은 그래프를 생성하지 않고 사유와 대체 정책 행동을 표시한다. 계산 가정이 없는 정책은 누락하지 않고 미산정 이유를 표시한다. 확정값 대안만 숫자 순위에 포함하고 조건부 대안은 순위·Pareto에서 제외한다.
5. **정책 상담**: 금액 계산 여부와 무관하게 공식 공고 근거의 후속 질문으로 이동할 수 있다.

#### 3.7.2. 순위와 대안을 탐색하는 방법

- 네 기준 카드는 사용자가 직접 입력하는 추천 가중치가 아니라, 서버가 이미 계산한 네 가지 관점의 결과다.
- 기준 카드를 누르면 아래 대안 카드와 표의 순서만 해당 기준으로 바뀌며, 사용자가 직접 고른 대안은 계속 선택된 상태로 남는다.
- 대안 카드·그래프 선·범례는 상호작용한다. 어느 하나를 호버하면 같은 대안이 강조되고, 클릭하면 선택 대안으로 고정된다.
- 대안 상세에서 13주·6개월 현금, 신규 부채, 월 최대 상환, 만기까지 이자, 실행 전 확인 조건, 공식 링크를 본다.
- `자격·접수 추가 확인`인 정책은 정보와 공식 링크는 보이되, 확정 대안인 것처럼 순위에 섞여 보이지 않게 상태를 구분한다.
- 조건부 정책은 정책별 미니 그래프와 전체 비교의 점선으로 표시하고 `조건부 가정·자격 및 승인 예측 아님·추천 순위 제외` 상태를 명시한다. 순위 제외 조건부 카드에는 사용자가 선택하지 않은 목표 판단값을 표시하지 않는다.

#### 3.7.3. 행동 브리프와 LLM 동의

- 기본 행동 브리프는 규칙·현금 계산 결과로 만든 로컬 문장이며 별도 동의 없이 바로 보여준다.
- 사용자가 선택 체크박스로 명시적으로 동의한 경우에만, 계산 금액과 선택 정책을 OpenAI API에 보내 문장을 다듬는다.
- LLM은 새 금액·자격·순위를 만들 수 없고, 서버가 재계산한 사실 범위 안에서만 문장을 다시 쓴다.
- 동의하지 않거나 API가 실패해도 로컬 행동 브리프, 비교 그래프, 순위, 공식 링크는 그대로 남는다.

#### 3.7.4. 정책 상담은 핵심 흐름 이후의 보조 기능이다

- 사용자는 선택한 정책 또는 전체 금융정책을 대상으로 최대 5회까지 질문할 수 있다.
- 무대응·비용 절감 대안을 고르면 전체 정책에서 찾고, 특정 정책 대안을 고르면 그 정책으로 검색 범위를 맞춘다.
- 답변은 공식 공고 근거와 다음에 확인할 조건을 제시하지만, 자격·승인을 확정하거나 현금 계산·순위를 바꾸지 않는다.
- 상담을 사용하지 않아도 1~4단계의 핵심 경험은 완결된다.

### 3.8. 상태·분기별 화면 반응

| 사용자 상태 | 화면이 보여줄 것 | 계산·순위 처리 | 다음 행동 |
|---|---|---|---|
| 필수 입력 누락 | 누락한 항목의 짧고 구체적인 오류 | 계산 시작 안 함 | 현재 화면에서 수정 |
| 계산 중 | 중앙 로딩, 현재 처리 문구, 0초부터의 경과 시간 | 중복 실행 방지 | 완료를 기다림 |
| 상권 AI 범위 사용 가능 | 하방·기준·회복 세 카드의 수치 | 선택 범위로 세 계산 중 하나를 표시 | 범위 선택 후 진단·비교 |
| 정책을 선택하지 않음 | 추천 후보와 선택 안내 | 무대응 기준선만 계산 | 정책 선택 또는 기준 결과 확인 |
| 선택 정책의 조건을 모름 | `추가 확인 필요`와 원 질문·현재 답변 | 검수된 가정이 있으면 조건부 그래프, 확정 순위 제외 | 문제 답변만 수정하거나 공식기관 확인 |
| 조건은 답했지만 금액·날짜를 모름 | 공식 지원 범위, 신청 준비 순서, 정책별 조건부 미니 그래프 | 검수된 범위·기간 가정이 있으면 조건부 점선을 계산하되 확정 순위·Pareto 제외 | 예상 효과 확인 후 공식 조건 보완 또는 상세 계산 열기 |
| 구조적 제외조건에 해당 | 원 질문·실제 선택 답변·제외 사유·대체 행동 | 조건부 그래프와 확정 순위에서 제외 | 답변이 잘못된 경우 해당 답변만 수정, 사실이면 다른 정책 비교 |
| 정책의 Event가 미검수 | 정책 정보·자격 상태·공식 링크 | 그래프·순위 제외 | 정보 후보로 활용 |
| 절감 계획이 없음 | 기본 입력판을 표시하지 않음 | 비용 절감 대안 생성 안 함 | 정책과 다음 행동 확인 |
| 확정 정책·절감액이 있음 | 무대응과 확정 대안의 카드·그래프·표 | 선택 기준으로 결정론적 순위 계산 | 대안 선택·상세·다음 행동 확인 |
| LLM 미동의·실패 | 로컬 행동 브리프 | 현금·자격·순위 불변 | 로컬 결과로 계속 진행 |

### 3.9. 화면 수정 시 지켜야 할 UX 원칙

1. **진단 뒤 질문, 정책 결과는 그 다음**: 첫 접속부터 질문하지 않고 현금 진단 뒤에 한 질문씩 확인한 다음 정책을 보여준다.
2. **질문은 한 장씩, 결과 정책은 동시에**: 질문은 연속 카드로 진행하되 답변 뒤의 정책 결과는 2~3개를 함께 비교할 수 있게 한다.
3. **AI와 계산기를 분리**: AI는 상권 범위와 정책 경로를 제시하고, 자격·현금·순위는 규칙·결정론 엔진이 계산함을 문구와 배치로 드러낸다.
4. **매출만이 아닌 전체 현금 구조**: 현금·임대료·인건비·매입비·기타 고정비·대출이 같이 계산된다는 점이 반드시 보여야 한다.
5. **점포 신호와 상권 신호를 분리**: 사용자 매출 흐름과 LightGBM 상권 범위를 하나의 `AI 예측`으로 합치지 않는다.
6. **확인된 값만 비교**: AI가 추정한 지원액·지급일·절감률을 자동 삽입하지 않는다.
7. **모름을 실패로 처리하지 않기**: 조건부 후보로 남기고 그 이유와 다음 확인 행동을 보여준다.
8. **선택과 결과의 연속성**: 상권 범위·비교 기준·선택 대안·상담 범위가 다른 화면으로 이동해도 예고 없이 초기화되지 않게 한다.
9. **상호작용이 보이게 하기**: 선·범례·카드 호버와 클릭, 선택 상태, 재계산 필요 여부를 색만이 아닌 문구·배지·초점으로도 표현한다.
10. **핵심 완결성은 LLM에 의존하지 않기**: 외부 AI 동의 없이도 입력 → 진단 → 정책 선택 → 비교 → 다음 행동이 끝나야 한다.

### 3.10. 현행 V2 화면·기능·코드 추적표

현행 연결의 중심은 다음과 같다.

`app/static/index.html` 화면 구조 → `app/static/app.js` 상태·이벤트·렌더링 → `app/main.py` HTTP API → `src/integration/re_stage8.py` 통합 조정 → 모델·검색·규칙·현금·정책 Event·추천 엔진 → API 응답 → JavaScript 카드·그래프·문장 표시

| 사용자 접점·기능 | 서비스 화면에서 나타나는 모습 | 프런트엔드 코드 | API·통합 코드 | 실제 계산·데이터 근거 |
|---|---|---|---|---|
| 서비스 진입·4단계 이동 | 상단 `사업장 선택 → 재무 입력 → 진단·정책 확인 → 비교·다음 행동`, 좌측 현재 입력 요약 | `index.html`의 `#business`, `#finance`, `#diagnosis`, `#decision`; `app.js`의 `showStep`, `updateSummary` | `/`, `/health`, `/scope`; `app/main.py` | 정적 화면과 현재 브라우저 세션 상태이며 별도 사용자 DB 저장 없음 |
| 상권·업종 목록과 지도 | 자치구·행정동·개별 상권 원, 검색, 목록, 선택 상태 동기화 | `loadCatalogs`, `initMap`, `chooseDistrict`, `chooseDong`, `selectArea`, `refreshAreaList` | `GET /api/v1/catalog/area-map`, `GET /api/v1/catalog/industries`; `area_map_catalog`, `industry_catalog` | `data/processed_re/re_stage8/commercial_area_points.json`, `reports/stage6/industry_catalog.csv` |
| 발표용 빠른 시연 5개 | 서로 다른 상권의 재무 입력을 채우되 자동 계산하지 않음 | `presentationPresets`, `applyPreset` | 프리셋 선택 시 API 호출 없음; 이후 진단 요청은 일반 사용자와 동일 | 프런트엔드에 고정된 5개 검증용 입력, 실제 사업장 아님 |
| 최근 매출·현금·비용·대출 입력 | 최근 3~12개월 매출, 현재 현금, 임대료·인건비·매입비·기타 고정비, 대출 조건 | `renderRevenueMonths`, `validateFinance`, `comparisonRequest` | `SampleCompareRequest.quick_input`; `compare_sample` | `src/cashflow/quick_mode.py`의 `QuickModeInput`, `build_quick_schedules`; 입력 금액은 원 단위로 변환 |
| 상권 하방·기준·회복 범위 | 세 범위 카드와 13주 현금 선, 선택 범위가 3·4단계에 공통 적용 | `loadMarketScenarios`, `selectScenario`, `renderCharts`, `updateScenarioApplicationStatus` | `GET /api/v1/market-scenarios/{area}/{industry}`, `POST /api/v1/alternatives/compare`; `market_scenario`, `compare_sample` | `src/models/re_stage5_scenario_service.py`; `market_features_2025q4.parquet`; `artifacts/re_stage5_lightgbm_quantile/` |
| 13주·6개월 현금 진단 | 현재 현금, 앞으로 28일 필요현금, 13주 뒤 현금, 6개월 현금·대출·이자, 필요현금 미달/적자 구간 | `runComparison`, `renderResults`, `drawChart`, `renderCharts` | `POST /api/v1/alternatives/compare`; `compare_sample` | `src/cashflow/engine.py`의 `run_detailed_cashflow`, `src/recommendation/engine.py`의 `suggest_safe_cash` |
| 점포 신호와 상권 AI 신호 분리 | `내 점포 최근 흐름`과 `상권·업종 AI 전망`을 별도 카드로 표시 | `renderStoreSignals` | 응답의 `external_scenario`, `market_scenario_comparison` | 점포 신호는 입력 매출의 최신·과거 비교, AI 신호는 집계 LightGBM 결과 |
| 질문 우선 단일 카드 | 한 화면에 질문 하나, `아니오·모름·예`, 선택·날짜·숫자, 이전 질문 | `initializeQuestionWizard`, `renderQuestionWizard`, `answerWizardQuestion`, `finishQuestionWizard` | 첫 비교 응답의 `policy_discovery.staged_questions`; 재응답의 `eligibility_profile` | `src/policy/discovery.py`의 `QUESTIONS`, `POLICY_FIELDS`, `staged_questions`; 검수된 고정 질문 Catalog |
| 상황 기반 정책 검색 | 질문 완료 뒤 점포 조건, 연결 이유, 공식 지원 범위, 정책 후보 최대 3개 표시 | `renderPolicyDiscovery`, `renderPolicyResults` | `compare_sample` → `_discover_policies` | `src/rag/hybrid_search.py`의 `HybridPolicySearchIndex`; `rag/index/policy_re8.sqlite3`; 17개 정책 817개 Chunk |
| 정책 자격·접수·신청 준비 | `현재 조건으로 지원 불가`, `준비하면 신청 가능`, `공식 확인 필요`, 원 질문·실제 답변·해결 행동 | `renderDecisionPolicyOpportunities` | `_resolved_eligibility_profile`, `_application_readiness`, `eligibility_results` | `src/policy/eligibility.py`의 10개 정책 56개 공식 Rule + `src/policy/discovery.py`의 7개 신규 정책 Overlay |
| 정책 선택 | 검색 결과 최대 3개 중 체크, 최초 결과에서 상위 2개 자동 선택, 최대 3개 유지 | `state.selectedPolicyIds`, `renderPolicyResults`, `[data-policy-select]` 변경 이벤트 | 요청의 `selected_policy_ids` | 선택은 세션 메모리이며 새 검색 결과·조건부 Event 범위를 제한 |
| 조건부 정책 현금효과 | 정책별 무대응 대비 미니 그래프, 전체 점선 그래프, 추천 순위 제외 | `enableSelectedPolicyPreviews`, `renderDecisionPolicyOpportunities`, `renderAlternatives`, `renderCharts` | 요청의 `conditional_policy_ids`; `_v2_conditional_policy_alternatives` | 서울시 육성자금·위기지원·재도전특별자금·대환대출의 검수 가정과 `src/policy` 변환기; 구조적 제외는 생성 차단 |
| 문제 답변 하나만 수정 | 4단계에서 해당 질문의 `이 답변 수정`을 누르고 답변 후 같은 정책 카드로 복귀 | `reviewWizardAnswers`, `finishQuestionWizard`, `[data-edit-policy-answer]` | 수정 답변을 포함한 비교 API 재호출 | `RULE_ANSWER_FIELDS`, `QUESTIONS`, 응답의 `blocking_details.answers`·`confirmation_details` |
| 선택형 상세 정책 계산 | 사용자가 이미 아는 금액·날짜만 입력한 정책 Event | `renderPolicyScenarios`, `policyScenarioPayload`, `openPolicyScenarioEditor` | 요청의 `policy_scenarios`; `_dynamic_policy_alternatives` | `src/policy/re_stage8_2_events.py`의 `DynamicPolicyScenario`, `build_dynamic_policy_plan`; 현재 화면 입력 지원 정책은 3개 |
| 선택형 비용 절감 계산 | 사용자가 이미 정한 항목별 월 절감액을 열어서 입력 | `setCostReductionEditor`, `costReductionPayload`, `updateConfirmedReduction` | 요청의 `cost_reduction_plan`; `_v2_cost_reduction_alternative` | 임의 5%가 아니라 입력한 네 비용 합계만 `compare_alternatives`에 반영 |
| 확정 대안 순위와 상세 | 무대응·확정 대안의 네 기준 순위, 13주·6개월 현금, 부채·상환·이자, 실행 확인사항 | `applyGoalRanking`, `renderGoalResults`, `renderAlternatives`, `openAlternative` | 응답의 `comparison_result`, `intervention_results`, `execution_plan` | `src/recommendation/engine.py`의 `compare_alternatives`; 가중합이 아닌 네 개의 고정 사전식 순위 |
| 로컬 행동 브리프 | 선택 대안의 핵심 수치와 첫 행동을 외부 AI 없이 즉시 표시 | `renderLocalActionBrief` | 비교 API 응답을 클라이언트가 요약 | 계산값을 다시 만들지 않는 로컬 결정론 문장 |
| 동의 기반 AI 행동 브리프 | 체크 후 `동의하고 AI 문장으로 정리`; 실패해도 로컬 브리프 유지 | `generateActionBrief` | `POST /api/v2/ai/action-brief`; `action_brief` | 서버가 비교를 재계산하고 `src/rag/luna_client.py`의 `explain_action_brief_with_luna`가 Fact Lock 안에서 문장만 재작성 |
| 정책 상담 채팅 | 전체 또는 특정 정책 범위, 최대 5회, 공식 링크·근거 표시 | `askPolicy`, `appendChatMessage`, `discoveredPolicyLinks` | `POST /api/v1/ai/ask`; `ask_policy` | Hybrid/BM25 검색 + `explain_with_luna`; 외부 모델 불가 시 로컬 근거 안내 Fallback |
| CSV 현금 진단 | 거래·대출 CSV로 별도 기준 현금 결과 확인 | `runCsvBaseline` | `POST /api/v1/cashflow/csv`; `parse_combined_csv`, `calculate_csv_baseline` | 업로드 문자열을 메모리에서 파싱하며 정책 대안 비교는 화면의 간편 입력 경로를 사용 |
| 로딩·오류·접근성 | 중앙 로딩과 실제 경과 시간, 토스트 오류, 키보드 카드 선택, `aria-live`, 다이얼로그 | `showLoading`, `hideLoading`, 전역 클릭·키보드 이벤트 | `app/main.py`의 요청 크기 제한과 정제된 검증 오류 | 서비스 의미와 무관한 공통 UI·안전 계약 |

### 3.11. 현재 정책 범위와 화면 노출 수

| 계층 | 현재 수 | 의미 |
|---|---:|---|
| 로컬 정책 DB | 17개 | 공식 Markdown과 Metadata가 색인된 전체 검토 정책 |
| 기본 금융 서비스 검색 범위 | 11개 | `FINANCIAL_POLICY_NEEDS`에 들어 있는 사업체 현금·대출·운영비·재기 정책 |
| 한 번의 서버 검색 후보 | 최대 6개 | `_discover_policies(... top_k=6)`의 내부 후보와 질문 생성 범위 |
| 3단계 정책 카드 | 최대 3개 | 우선 후보와 기존 선택 정책을 합쳐 화면에 표시 |
| 사용자가 선택 가능한 정책 | 최대 3개 | `selected_policy_ids` 요청 한도 |
| 검수된 자동 조건부 그래프 | 4개 정책 | 서울시 육성자금, 위기지원, 재도전특별자금, 대환대출 |
| 사용자 상세 Event 입력 UI | 3개 정책 | 위기지원, 자영업자 고용보험료 지원, 가족친화 기업지원 |

17개 정책이 모두 같은 깊이로 현금 그래프까지 연결된 것은 아니다. `검색 가능`, `자격 Rule 가능`, `사용자 입력 Event 가능`, `검수된 조건부 그래프 가능`은 서로 다른 구현 범위다.

### 3.12. AI·규칙·계산 엔진의 실제 역할

| 구성요소 | AI 여부 | 입력 | 실제 출력 | 서비스에서 보이는 위치 | 할 수 없는 일 |
|---|---|---|---|---|---|
| LightGBM Quantile | ML | 상권 코드, 업종 코드, 2025년 4분기 집계 Feature | 13주·6개월 하방·기준·회복 변화율 | 3단계 상권 AI 전망 카드와 세 현금 선, 4단계 적용 범위 | 개별 점포 매출·폐업·승인 예측 |
| Hybrid 정책 검색 | 검색 AI | 자치구·업종·매출 추세 Band·고정비/부채/현금위험 같은 범주형 상황 | 관련 공식 정책 Chunk와 정책 후보 순서 | 3단계 질문 Catalog의 후보 범위와 답변 후 정책 카드 | 자격 확정, 지원액·지급일 생성, 순위 결정 |
| BM25 Fallback | 비생성 검색 | 같은 범주형 검색 문장 | 키워드·한글 문자 기반 정책 후보 | Hybrid 외부 Embedding을 사용할 수 없을 때 동일 화면 유지 | 의미 벡터 검색 |
| 공식 Rule 엔진 | 결정론 | 사용자 답변, 자치구, 업종, 기준일 | pass·fail·unknown, 적격·추가 확인·제외, 원 질문·실제 답변 | 질문 카드와 4단계 신청 준비 카드 | 승인확률, 예외 인정, 최신 잔여 예산 확정 |
| 현금흐름 엔진 | 결정론 | 매출·지출·대출 일정과 정책 Event | 13주·6개월 현금, 최저 현금, 부채·상환·이자 | 진단·미니 그래프·전체 그래프·수치표 | 인과효과 보장, 미래 매출 생성 |
| 정책 Event 엔진 | 결정론 | 검수 정책 조건과 사용자 확정값 또는 공개된 조건부 가정 | 보조금·융자·대환의 현금·부채 일정 | 확정 대안 또는 점선 조건부 대안 | 미검수 금액·날짜 추정 |
| 추천 엔진 | 결정론 | 동일 현금 시나리오의 실행 가능한 대안 | 최소부채·최장생존·최소상환·빠른실행의 네 순위와 Pareto | 4단계 목표 카드·대안 순서·상세표 | 사용자 선호 가중치 학습, 조건부 정책 순위 포함 |
| 로컬 행동 브리프 | 결정론 | 선택 대안의 계산·자격·실행계획 | 핵심 수치와 다음 행동 문장 | 4단계 최상단 | 새로운 근거·자격·금액 생성 |
| Luna 설명 | 생성형 AI, 선택 | 서버가 재계산한 사실과 검색한 공식 근거 | 읽기 쉬운 행동 브리프 또는 상담 답변 | 동의 기반 행동 브리프·정책 상담 | 계산·자격·순위 수정, 공식 근거 밖의 단정 |

현재 로컬 검토 서버는 `OPENAI_API_KEY`를 빈 값으로 실행한다. 2026-08-23 내부 API 확인에서 정책 검색은 `bm25_fallback`, 사유는 `OpenAIEmbeddingError`였고, LightGBM 상권 시나리오·규칙·현금·추천 엔진은 정상 동작했다. 따라서 **현행 로컬 화면에서 실제 작동 중인 AI는 LightGBM이며, 정책 벡터 검색과 Luna 생성은 비활성 Fallback 상태**라는 사실을 대회용 설명과 구분해야 한다.

### 3.13. 사용자 행동별 상태 변화

| 사용자 행동 | 브라우저 상태 변화 | 서버 재호출 | 결과에 미치는 영향 |
|---|---|---|---|
| 빠른 시연 선택 | 사업장·업종·6개월 재무값 채움, 기존 결과 초기화 | 없음 | `현금 진단 보기` 전에는 결과 없음 |
| 현금 진단 보기 | 로딩 시작, 세 상권 범위 결과 저장 | 시장 API 1회 + 비교 API 3회 | 진단·질문 Catalog 생성 |
| 질문 답변 | `eligibilityAnswers` 갱신, 다음 카드 이동 | 마지막 질문 전에는 없음 | 마지막 질문 후 정책 재검색·자격 재평가 |
| 전체 답변 수정 | 전체 질문 순서로 복귀 | 마지막 수정 완료 후 비교 재호출 | 정책 후보·상태 전체 갱신 |
| 4단계 문제 답변 수정 | 해당 필드 하나만 질문 | 수정 완료 후 비교 재호출 | 같은 정책 카드로 복귀, 그래프 허용 여부 재평가 |
| 정책 체크 | 선택 Set 변경, 최대 3개 검증 | 즉시 없음 | 다음 재계산의 정책·Event·조건부 범위 변경 |
| 조건부 그래프 보기·제거 | 조건부 정책 Set 변경 | 비교 API 3회 | 점선·미니 그래프 추가 또는 제거, 순위 불변 |
| 4단계로 이동 | 허용된 선택 정책을 조건부 Set에 자동 반영 | 비교 API 3회 | 신청 준비 카드와 조건부 효과 생성 |
| 상권 범위 변경 | 선택 범위와 캐시 변경 | 필요한 경우 비교 재호출 | 3·4단계 현금값과 그래프 모두 변경 |
| 목표 기준 변경 | 목표와 선택 대안 상태 갱신 | 없음 | 서버가 미리 반환한 네 순위 중 하나로 로컬 재정렬 |
| 상세 정책값·절감액 변경 | 입력 상태와 캐시 무효화 | `다시 계산` 클릭 시 | 확정 Event·비용 대안 생성 또는 제거 |
| LLM 행동 브리프 요청 | 동의 확인, 로딩 | 행동 브리프 API 1회 | 서버 재계산 사실의 문장만 변경 |
| 정책 상담 전송 | 대화 회수 증가, 질문 기록 | 상담 API 1회 | 계산·자격·순위는 바뀌지 않음 |

현재 `runComparison`은 하방·기준·회복에 대해 비교 API를 3회 호출한다. 각 비교 응답도 `market_scenario_comparison`에 세 범위의 무대응 경로를 포함하므로 계산이 일부 중복된다. 기능 오류는 아니지만 로딩 시간과 서버 연산을 분석할 때 고려할 기술 부채다.

### 3.14. 대회 목적·AI 강화 재비교를 위한 현재 기준선

| 대회·심사 관점 | 현재 V2에서 확인되는 근거 | 비교할 때 남겨야 할 질문 |
|---|---|---|
| 구체적인 금융 현안 | 현금 부족 소상공인이 정책을 찾더라도 자격·금액·지급시점과 현금효과를 연결하기 어려운 문제를 다룸 | 문제의 빈도·규모를 외부 근거로 충분히 입증했는가 |
| 특정 금융소비자·채널 | 서울 소상공인, 계정 없는 로컬 웹 4단계 안내 | 실제 사용자가 질문 수와 금융 용어를 감당할 수 있는가 |
| 맞춤형 AI 서비스 | 상권·업종 ML 범위와 상황 기반 정책 검색을 점포 재무·자격 답변에 연결 | 질문이 답변에 따라 줄어들지 않는 현재 구조가 `맞춤형`으로 충분한가 |
| 생성형 AI 적용 | 동의 기반 Fact Lock 행동 브리프와 공식 공고 상담 | LLM이 선택 기능에 머물러도 AI 필연성이 설득되는가, 더 강한 역할이 안전하게 필요한가 |
| 실제 작동 웹서비스 | 지도·입력·진단·질문·정책·그래프·비교·상담이 API와 연결됨 | 현재 로컬 Fallback 상태와 향후 제출 실행환경의 AI 가용성이 일치하는가 |
| 데이터 활용 | 상권 집계 Feature·LightGBM Artifact·공식 정책 SQLite·검수 Rule/Event가 파일로 추적됨 | 데이터 최신성·정책 11개 서비스 범위·조건부 그래프 4개의 제한을 어떻게 설명할 것인가 |
| 금융 신뢰·안전 | 개별 예측·승인 보장 금지, 조건부 순위 제외, 구조적 제외 그래프 차단 | 보수적 제한이 사용자에게 도움 부족으로 느껴지지 않는가 |
| 사용자 경험 | 진단 후 한 질문씩, 정책 최대 3개, 문제 답변만 수정, 신청 준비와 효과 연결 | 초기 후보 기준 질문 수가 길지 않은가, 상위 2개 자동 선택이 사용자 의도와 맞는가 |
| 검증 가능성 | Python 회귀, JavaScript 문법, 로컬 HTTP·API 상태 검증 | 실사용자 검증과 모바일·키보드 최종 화면 검증이 아직 없다는 한계를 어떻게 보완할 것인가 |

이 표는 우승 가능성이나 AI 강화 방향을 미리 결론내리는 표가 아니다. 다음 비교에서 `대회 목적 적합성`, `AI가 실제로 추가한 가치`, `결정론 엔진만으로 가능한 부분`, `생성형 AI가 꼭 필요한 부분`, `사용자 마찰`을 같은 기준으로 평가하기 위한 현행 기준선이다.

### 3.15. 현재 구현의 핵심 한계·분석 주의사항

1. **화면은 질문 우선, 내부는 검색 우선**이다. 질문을 만들기 위해 답변 전 정책 검색을 수행한다.
2. **질문은 후보 맞춤형이지만 완전 적응형은 아니다.** 첫 후보군의 미입력 질문을 정한 뒤, 각 답변마다 서버가 다음 질문을 다시 선택하지 않는다.
3. **정책 선택은 완전 수동이 아니다.** 최초 정책 결과에서 상위 2개가 자동 선택된다.
4. **정책 구현 깊이가 다르다.** 11개 기본 검색 정책 모두가 조건부 그래프나 상세 Event 입력을 제공하지 않는다.
5. **확정 대안과 조건부 효과는 다른 계층이다.** 조건부 점선은 동기 부여용 미리보기지만 자격·승인 예측이나 추천 순위가 아니다.
6. **현재 로컬 서버는 벡터 검색·Luna가 아닌 Fallback 상태다.** 이를 전체 AI 스택의 실시간 실행으로 과장하면 안 된다.
7. **비교 API 호출과 세 범위 계산이 중복된다.** 기능은 맞지만 발표 시 체감 지연과 서버 비용을 따로 평가해야 한다.
8. **V2-008 선택형 입력 배치는 시험 상태다.** 사용자 확정 전에는 최종 UX 계약으로 간주하지 않는다.
9. **화면 최종 판정은 사용자 소유다.** 자동 테스트와 DOM·API 검증을 실제 가독성·모바일 사용성 검증으로 바꾸어 말하지 않는다.
10. **실제 소상공인 사용성 검증은 없다.** 현재 검증은 가상 시나리오와 내부 계약 회귀다.

## 4. V2 구현 단계

### V2-0. 기준선 동결과 보존 검사

- [x] MVP 계획표를 별도 이력 문서로 보존
- [x] 지도·예시·그래프·로딩·CSV의 DOM ID와 이벤트 목록 확인
- [x] 기존 집중 테스트 기준선 확인
- Gate: 보존 계약과 현재 파일 변경 상태가 기록돼야 한다.

### V2-1. V2 정보 구조와 첫 화면

- [x] 서비스 가치를 한 문장으로 설명하는 랜딩 영역
- [x] AI 구성요소의 역할과 한계를 사용자 언어로 표시
- [x] 4단계 진행 구조를 진단 → 정책 확인 → 비교 흐름에 맞게 재명명
- Gate: 첫 화면만 보고 입력·결과·AI 역할을 설명할 수 있어야 한다.

### V2-2. 재무 입력 완성

- [x] 기타 고정비 입력을 UI·간편 입력 스키마·현금 Event에 연결
- [x] 발표용 5개 예시에 기타 고정비 값 추가
- [x] 최근 점포 매출 추세를 평균 매출과 분리해서 계산·표시
- Gate: 월 지출 합계에 네 비용 항목이 모두 포함되고 기존 입력도 호환돼야 한다.

### V2-3. 정책 경로 탐색

- [x] 질문 완료 전에는 정책 결과를 숨기고 마지막 답변 후 기존 검색을 다시 실행
- [x] 각 후보에 ‘왜 찾았는지’, 자격 상태, 공식 공고를 표시
- [x] 사용자가 비교할 정책을 최대 3개까지 명시적으로 선택
- Gate: 질문 답변을 반영한 검색 뒤에 정책 경로를 발견하고 선택할 수 있어야 한다.

### V2-4. 적응형 조건 확인

- [x] 3단계 안에서 한 화면에 질문 한 개만 표시
- [x] 선택 즉시 다음 질문으로 이동하고 이전 질문·답변 수정 지원
- [x] 예·아니오·모름, 선택형, 날짜, 숫자 질문을 한 장 구조로 처리
- [x] 마지막 답변 후 기존 정책 결과·금액 입력·4단계 흐름에 연결
- Gate: 질문이 동시에 줄줄이 노출되지 않고, 모름은 조건부 상태로 유지되며, 완료 후 기존 정책 결과로 이동해야 한다.

### V2-5. 사용자 확정 시나리오 (V2-008 시험 배치, 미확정)

- [x] 기본 흐름은 정책 금액·날짜·절감액 없이 4단계까지 진행
- [x] 13주 추가 필요 현금은 기존 28일 필요현금과 13주 최저 현금의 차액으로 계산
- [x] 미확정 정책은 4단계 다음 행동 후보로 유지하고 숫자 순위에서 제외
- [x] 정책 금액·날짜와 비용 절감 입력은 사용자가 직접 연 선택형 상세 계산에서만 표시
- [x] 비용 절감은 임대료·인건비·매입비·기타 고정비의 실제 절감 가능액으로 입력
- [x] 절감액이 0원이면 비용 절감 대안을 생성하지 않음
- [x] 현금 Event 지원 정책은 필요한 금액·등급·지급일을 모두 입력한 경우만 생성
- [x] 금액·날짜가 없는 정책은 정보 후보로 유지하고 순위에서 제외
- [x] 세부 자금 미선택은 하드 부적격이 아니라 신청 준비 상태로 분리
- [x] 구조적 제외·보완 가능·공식 확인 필요 상태와 원 질문·실제 답변·해결 행동을 구분
- [x] 실패·미확인인 문제 답변만 수정하고 재계산 후 같은 4단계 정책 카드로 복귀
- [x] 보완 가능·공식 확인 필요 정책만 검수된 조건부 그래프를 자동 계산하고 구조적 제외는 그래프 차단
- [x] 조건부 대안은 목표 판단값 대신 `조건부 현금효과·추천 순위 제외`를 표시
- 시험 Gate: 기본 흐름에서 추가 금액 입력을 강제하거나, 고정 5% 비용 절감 또는 임의의 지원금·지급일이 V2 순위에 들어가면 실패다. 사용자 검토 뒤 유지·수정·폐기할 수 있다.

### V2-6. 동일 기준 대안 비교

- [x] 무대응과 사용자 확정 대안만 동일한 13주·6개월 축에서 비교
- [x] 목표별 순위, 부채, 월 상환, 현금 고갈, 확인 항목 표시
- [x] 조건부·접수 미확인 정책은 그래프와 순위 상태를 명확히 구분
- Gate: 화면에 나타난 모든 순위가 결정론적 엔진 결과와 일치해야 한다.

### V2-7. 제한형 AI 행동 브리프

- [x] 계산 결과와 공식 근거로 다음 행동을 자동 정리
- [x] 외부 LLM 사용 전 전송 범위와 저장하지 않음을 알리고 동의 받기
- [x] LLM은 계산·자격·순위를 생성하거나 수정할 수 없도록 Fact Lock 유지
- [x] LLM 미사용·실패 시 로컬 결정론적 브리프 제공
- Gate: LLM을 끄거나 실패시켜도 핵심 의사결정과 다음 행동이 남아야 한다.

### V2-8. 회귀·발표 검증

- [x] Python 전체 테스트 및 V2 신규 테스트
- [x] JavaScript 구문 검사
- [x] 지도·5개 예시·두 그래프·로딩의 보존 회귀 검사
- [ ] 모바일·키보드·빈 상태·오류 상태 확인
- [x] 실제 사용자 검증이 없다는 한계를 발표 문구에 유지
- Gate: 필수 기능이 통과하고, 미검증 항목은 실제 검증으로 표현하지 않아야 한다.

> 2026-08-21 확인: 자동 테스트와 정적 반응형 규칙 검사는 통과했다. 인앱 브라우저 플러그인의 신뢰 경로 초기화 오류로 실제 화면·모바일 폭 스크린샷 검사는 보류했다.

## 5. 평가 기준

현실 사용자 검증 없이 확인 가능한 범위만 평가한다.

- **기능 적합성**: 준비된 시나리오에서 입력 → 진단 → 질문 → 정책 확인·선택 → 신청 준비·비교 → 행동 브리프가 끝까지 동작하는가.
- **AI 필연성**: LightGBM·Hybrid 검색·선택형 LLM이 결정론적 규칙·현금·추천 엔진으로 대체할 수 없는 역할을 실제로 맡는가.
- **금융 타당성**: 현금·부채·상환액·지급시점이 같은 축에서 계산되는가.
- **신뢰성**: 개별 점포 예측, 승인 보장, 임의 금액 생성처럼 근거를 넘는 주장을 하지 않는가.
- **사용 경험**: 긴 설문보다 진단을 먼저 보여주고, 필요한 질문만 이어서 제시하는가.
- **보존성**: MVP에서 검증한 지도·시연·그래프·로딩을 잃지 않았는가.

## 6. 현행 분석 결과와 다음 비교 기준

기존 `현재 구현 우선순위` 여섯 항목은 모두 구현됐으므로 더 이상 작업 순서로 사용하지 않는다. 현행 V2의 다음 판단은 코드를 추가하기 전에 아래 질문으로 수행한다.

1. **대회 목적 적합성**: 소상공인의 정책 탐색 문제가 아니라 `현금 위기에서 실행 가능한 금융 행동을 선택하는 문제`로 충분히 선명한가.
2. **AI 강화 필요성**: 후보 맞춤형 고정 질문을 답변 기반 동적 질문으로 바꾸는 것이 실제 질문 수와 이탈을 줄이는가.
3. **검색 AI 기여**: Hybrid Vector가 BM25보다 사용자에게 더 적절한 정책과 이유를 제공하는지 현재 평가 결과와 실제 화면 사례로 증명할 수 있는가.
4. **상권 ML 기여**: LightGBM 하방·기준·회복 범위가 무대응·정책 효과 판단을 실제로 바꾸며, 단순 고정 충격보다 나은가.
5. **생성형 AI 필연성**: 선택형 행동 브리프·상담을 유지할지, 신청 준비 행동을 더 개인화할지, 또는 현재 안전 경계를 유지할지 비교한다.
6. **정책 커버리지**: 11개 검색 정책 중 조건부 그래프 4개와 상세 Event 3개의 깊이 차이가 핵심 서비스 설명을 약하게 만드는지 확인한다.
7. **사용자 마찰**: 질문 수, 상위 2개 자동 선택, 3단계와 4단계의 정보량, 조건부·확정 구분이 실제 사용에서 이해되는지 사용자가 직접 점검한다.
8. **실행 성능**: 세 비교 요청과 중복 시나리오 계산을 줄였을 때 로딩 경험이 개선되는지 확인한다.
9. **검증 수준**: 내부 회귀·가상 시나리오를 넘어 소상공인 또는 대체 사용자 검증을 어떤 방식으로 확보할지 결정한다.
10. **제출 실행환경**: 현재 로컬 BM25 Fallback과 제출 환경의 Embedding·Luna 사용 여부, 비용·동의·장애 Fallback을 같은 계약으로 맞춘다.

이 문서의 3.10~3.15가 이후 `대회 목적 재평가`와 `AI 강화안 비교`의 기준 원장이다. 기능 또는 의미를 바꾸는 선택은 별도 사용자 승인 후 V2 수정 ID로 구현한다.
