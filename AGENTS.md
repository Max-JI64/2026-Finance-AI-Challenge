# Project Agent Rules

## Mandatory user decisions

- 작업 중 결과의 의미·범위·처리 기준을 실질적으로 바꾸는 선택이 필요하면 실행 전에 멈춘다.
- 선택지, 영향, 권장안을 사용자에게 설명하고 명시적인 허락을 받은 뒤 재개한다.
- 필수 확인 대상에는 Target 정의·임계값, 데이터 포함·제외, 결측·이상치 처리, 학습·검증 기간, 평가 지표, 모델 최종 선택, 서비스 범위 변경, 외부 전송·배포, 되돌리기 어려운 작업이 포함된다.
- 기존의 포괄적인 진행 요청을 이후에 새로 발생한 중대한 선택에 대한 허락으로 간주하지 않는다.
- 결과를 바꾸지 않는 통상적인 구현 세부사항과 읽기 전용 확인은 합리적으로 판단해 계속 진행할 수 있다.
- 사용자 선택이 필요하지 않으면 불필요하게 확인을 반복하지 말고 현재 승인 범위에서 작업을 완료한다.

## Project continuity

- 세션 시작 시 루트 `AGENT_MEMORY.md`에서 기존 결정과 현재 인계 상태를 확인한다.
- 발표·포트폴리오용 작업 이력은 루트 `LOG.md`에 KST 분 단위로 기록한다.

## Proportional verification

- 각 Stage의 필수 Gate와 결과 정확성에 직접 영향을 주는 항목을 우선 검증한다.
- 필수 조건이 통과했다면 비필수·미관·부가 검증의 실패만으로 같은 작업을 완벽해질 때까지 반복하지 않는다.
- 비필수 실패는 짧게 기록하고 다음 단계로 진행한다.
- 재검증은 필수 Gate 실패, 산출물 손상, 핵심 수치 불일치, 재현 불가처럼 결과 신뢰성을 훼손하는 경우에만 수행한다.
- 이미 통과한 동일 검증을 특별한 변경이나 실패 증거 없이 반복하지 않는다.
