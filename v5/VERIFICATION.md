# V5 검증 기록

검증 시각: 2026-08-26 22:54 KST

## 결과 요약

V5-0부터 V5-7까지 코드와 비시각 Gate를 통과했다. V5는 V4에서 독립 복사됐고, V4와 공용 `src/`는 수정하지 않았다. 최종 화면의 시각적 완성도, 모바일 체감, 발표 3분 리허설은 사용자 승인 범위로 남아 있다.

| 검증 | 결과 |
| --- | --- |
| V4 구현 전 기준선 | `v4/tests/test_v4.py` 16/16 통과 |
| V4 복사 직후 일치 | 16/16 파일 SHA-256 일치 |
| V5 전용 회귀 | 17/17 통과 |
| V4와 관련 공용 정책 회귀 | 83/83 통과 |
| 사용자 피드백 후 V3·V4 집중 회귀 | 30/30 통과 |
| 검토 렌즈 Oracle | 25/25 일치 |
| 정책별 준비 조건 계약 | 후보별 전체 필드 일치 |
| 고정 사용자 흐름 계약 | 8/8 일치 |
| JavaScript 문법 | `app.js`, `v5-extension.js` 2/2 통과 |
| Python 문법 | `main.py`, `orchestrator.py`, `copilot.py`, `test_v5.py` 통과 |
| V4 원본 불변 | 복사 기준 16/16 SHA-256 유지 |
| V5 라이브 상태 | PID 24416, `127.0.0.1:8003`, `/health` HTTP 200 |

## Stage Gate

### V5-0. 독립 기준선

- `v4/` 승인 소스 16개를 `v5/`로 복사했다.
- 캐시, 로그, `__pycache__`, V4 결과 기록은 복사하지 않았다.
- 복사 직후 16/16 해시가 일치했으며 `V4_COPY_BASELINE_SHA256.md`에 기록했다.
- V5 자산명, API 접두사, 버전, 세션키, 포트와 공고 캐시 경로를 분리했다.

### V5-1. 문서와 화면 셸

- 상단 진행은 `진단`, `비교`, `준비` 3개다.
- 해결 목적 5개는 단일 선택이다.
- 정상 경로의 발표 프리셋은 숨기고 `?demo=1`에서만 표시한다.
- 프리셋은 입력만 채우며 목적 선택이나 계산을 자동 실행하지 않는다.
- 계산에 연결되지 않던 운영 기간 입력을 제거했다.

### V5-2. 검토 렌즈와 계획

- 현금 생존, 부채 부담, 비용 절감, 정책 유형 비교와 `unsure`를 엄격한 Schema로 제한했다.
- `unsure`는 진단 신호에 따른 기준을 제안하되 사용자 확인 전 검토 순서를 생성하지 않는다.
- 계획에는 목적, 사용자가 이해할 수 있는 재무 상태와 먼저 볼 효과만 포함한다.
- 감지 신호와 정책 지원방식은 내부 키 대신 금액과 의미가 포함된 한국어로 표시한다.
- 내부 설계 설명인 `질문 기준`과 `이번 선택이 실제로 바꾼 순서` 카드는 제거했다.
- 렌즈를 바꾼 두 실제 API 응답에서 후보, 자격, 시나리오, 정책 효과, 기존 순위 권한 해시가 모두 일치했다.

### V5-3. 정책별 자격조건 선택

- 진단과 비교 사이의 질문 게이트를 제거해 정책 후보를 바로 보여준다.
- 선택한 정책의 준비 화면에서 그 정책에 연결된 전체 검수 조건을 직접 선택지로 표시한다.
- 조건 선택은 같은 준비 화면을 유지한 채 자격 상태와 조건부 정책 효과를 다시 계산한다.
- 사용자가 답할 수 없는 접수 상태와 잔여 예산 등은 `기관에서 확인할 항목`으로 분리한다.
- 기존 질문 계획기와 질문 Trace는 실행 경로에서 제거했다.

### V5-4. 정책 비교

- 기존 `rank_position`과 V5 `review_position`을 분리했다.
- 사용자가 선택한 최대 3개 안에서만 금융 메커니즘별 검토 순서를 만든다.
- 검토 1번 정책, 이유와 렌즈별 지표 순서를 표시한다.
- 새 추천 점수나 금융 결과를 만들지 않는다.

### V5-5. 다음 공고 확인

- V4의 여섯 공고 필드와 필드별 근거 Fail-close를 유지했다.
- V5 캐시는 `v5/runtime/notice_extraction_cache.sqlite`만 사용한다.
- 신청 기간을 첫 항목으로 한 개만 펼치고, 나머지는 접힌 상세로 둔다.
- 분석 요청 중에는 회전 표시와 `AI가 공고를 확인하는 중` 문구를 노출한다.
- 완료 화면에서는 공고 조각 수와 원문 해시를 숨기고, 확인 수가 정책 개수가 아니라 선택 정책 한 건의 신청정보 여섯 항목임을 명시한다.
- 모든 항목을 읽으면 이 화면의 확인만 끝났다고 알리고 현재 접수 여부 확인과 정책 비교 복귀 행동을 제공한다.
- 확인 상태는 공고 원문 해시와 함께 브라우저 세션에 저장한다.

### V5-6. 하네스와 평가

- 입력, 검토 계획, 검토 순서, 공고 우선순위와 도구 Trace에 엄격한 Pydantic Schema를 적용했다.
- 금융 권한 해시와 도구 버전, 입출력 Schema 버전, 상태, 표시 이유를 응답에 남긴다.
- 고정 평가는 검토 렌즈 25개와 사용자 흐름 8개를 유지하고, 정책별 준비 조건이 공용 `POLICY_FIELDS` 계약과 정확히 일치하는지 회귀 테스트한다.
- 기존 질문 계획 30개 파일은 이전 설계 이력으로 보존하지만 현재 실행 경로의 성공 기준에는 포함하지 않는다.

### V5-7. 회귀와 실행

실행한 핵심 명령은 다음과 같다.

```powershell
node --check v5/static/app.js
node --check v5/static/v5-extension.js

$p = Start-Process -FilePath 'C:\Program Files\Python313\python.exe' -ArgumentList @('-m','py_compile','v5\main.py','v5\orchestrator.py','v5\copilot.py','v5\tests\test_v5.py') -WorkingDirectory 'D:\대회\2026 금융 AI Challenge' -Wait -PassThru -NoNewWindow

$p = Start-Process -FilePath 'C:\Program Files\Python313\python.exe' -ArgumentList @('-m','pytest','v5\tests\test_v5.py','-q') -WorkingDirectory 'D:\대회\2026 금융 AI Challenge' -Wait -PassThru -NoNewWindow

$p = Start-Process -FilePath 'C:\Program Files\Python313\python.exe' -ArgumentList @('-m','pytest','v4\tests\test_v4.py','tests\test_re_stage4_policy_events.py','tests\test_re_stage8.py','tests\test_re_stage8_2.py','-q') -WorkingDirectory 'D:\대회\2026 금융 AI Challenge' -Wait -PassThru -NoNewWindow
```

공용 회귀에는 LightGBM 학습 시 feature name 경고 4건이 있었지만 테스트 실패나 계산 불일치는 없었다.

2026-08-26 사용자 화면 피드백 반영 뒤 앞단 질문 게이트를 제거하고 준비 화면의 정책별 선택지로 통합했다. V5 전용 17/17과 V3·V4 집중 회귀 30/30을 다시 통과했다. 전체 테스트 묶음에서는 이번 V5 변경과 무관한 `reports/re_stage8_3/service_review_log.md`의 현재 해시와 보존된 RE9 Manifest 해시가 다른 기존 실패 1건만 확인했다. 보존 문서나 Manifest를 임의로 되돌리거나 갱신하지 않았다.

인앱 브라우저의 실제 흐름에서도 앞단 질문 영역 0개, 정책 카드 3개 즉시 표시, 선택 정책의 준비 조건 카드 4개, 직접 선택 버튼 11개, 이전 `답변 수정` 버튼 0개를 확인했다. 조건 하나를 선택한 뒤 준비 화면을 유지하면서 선택 상태가 반영되는 것도 확인했다.

## 라이브 확인

- 실행: `v5.main:app`
- 주소: `http://127.0.0.1:8003/`
- 프로세스: PID 24416
- `/health`: HTTP 200, `v5-api-v1.0`
- `/`: HTTP 200, V5 JavaScript 자산 확인

## 수행하지 않은 항목

- 외부 AI 호출과 새 외부 데이터 획득
- 사용자 재무정보의 외부 전송 또는 장기 저장
- 배포, 기관 전송, 자동 신청
- 화면 캡처 기반 최종 시각 승인
- 모바일 실제 기기 체감 검수
- 발표 시나리오 3분 리허설

마지막 세 항목은 코드·정적 검사 결과로 승인했다고 간주하지 않는다.
